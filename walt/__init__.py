"""walt — exact endgame information-set solver vs a deterministic field.

Regular package (not a namespace package): all walt.* submodules resolve from
THIS directory, whichever checkout it lives in. Sibling packages (forge,
champion, arena, gus) resolve from the same repo root via normal sys.path —
run from the repo root; no path hacks anywhere in walt.
"""
