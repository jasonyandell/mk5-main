Reviewed against code on 2026-07-07 — no issues found.

- Results table matches the 43009a4 commit message line-for-line; adapter repo `jasonyandell/gemma-4-e2b-texas42-stage0-kerry` matches `ADAPTER_REPO` in `lem/gemma_star/train_stage0.py`; 15k corpus confirmed by f8cdbe7.
- The raw inference transcript itself lives off-repo (HF adapter + interactive run) — unverifiable here; only the commit-message summary was checkable.
- Cheap next probe: a targeted 6-4-under-fives membership battery (all 7 count/non-count dominoes containing a 4-pip) would isolate whether the residual error is pip-confusion or count-salience.
