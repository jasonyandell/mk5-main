Reviewed against code on 2026-07-07 — no issues found.

- All six proxy-bucket regret numbers (deltas, CIs, n's) match `w42/setter_defense_claim_validation/claim_proxy_stats.csv` exactly; seeds/command/commit match `summary.json`.
- Superseding numbers (+4.102 / +6.069 / -8.372, 28,000 decisions) match both `w42-gus-corpus-tactical-claim-deep-dive` and `w42-tactical-claim-replication` pages.
- Cheap next probe: the `ch05-trump-rich-count-intervention` slice has only 2 paired decisions — the gus v2 generated corpus (28k rows) could power it the same way pounce was replicated.
