Reviewed against code on 2026-07-07 — no issues found.

- All 14 paired-contrast deltas and Ns match `w42/sequence_seat_counterfactuals/paired_contrasts.csv` exactly; phase/position slice numbers match `paired_contrasts_by_slice.csv`; coverage counts (75079 / 28000 / 14 / 394) match `summary.json`; W&B run id `kp0otpdo` confirmed in `summary.json`.
- Note: the input file `w42/tactical_claim_replication/all_action_rows.jsonl` is not present in the worktree (generated artifact, likely gitignored); `summary.json` confirms it was the input at run time.
- Cheap next probe: the partner-support trick-timing pattern (trick 3 = +0.994 sits between the "negative/uncertain" 1-2 and "stronger positive" 4-5) could be re-cut by remaining-count-inventory instead of trick index to test whether timing is really inventory depletion.
