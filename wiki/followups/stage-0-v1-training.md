Reviewed against code on 2026-07-07 — no issues found.

- `lem/rules/qa_corpus.jsonl` is a generated artifact (default output of `lem/rules/generate_qa.py`) and is not checked in; page phrasing is fine, but a reader may look for the file — a parenthetical "(generated by generate_qa.py)" would help.
- Training metrics (loss curve, 208 steps, ~60 min, wandb run) live on Modal/W&B and are unverifiable from the repo; they match the df73c8d commit-message digest, which is the best in-repo evidence.
- Note the script defaults to 3 epochs (`epochs: int = 3` in train_stage0.py) while the run used 1; worth stating explicitly if the recipe is meant to be copy-paste reproducible.
