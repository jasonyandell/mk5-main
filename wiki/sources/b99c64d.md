---
title: "Source digest: b99c64d — batch narration generator + A100 training + eval seeds"
kind: source
first_seen: 2026-04-10
last_updated: 2026-04-10
status: active
---

## Commit

- **SHA:** b99c64d041cc16a095c17c542596a01615cd631b
- **Date:** 2026-04-10
- **Author:** Jason Yandell

> feat(lem): batch narration generator + A100 training + eval seeds
>
> Batch generator (lem/narrate/batch.py):
> - Processes N seeds × 10 decls × 4 perspectives in one pass
> - Filters to trick-6 decisions: |legal|>=2, E[Q] gap >= threshold
> - Outputs JSONL with full grading data (bot_eq, best_eq, all_eq, legal_actions)
> - 200 seeds → 3148 train examples in 8.5 min on 3050 Ti
> - Eval seed protection (900000-909999) with --allow-eval-seeds override
>
> Generated datasets:
> - lem/data/narrations_train.jsonl: 3148 examples from seeds 0-199
> - lem/data/narrations_eval.jsonl: 812 examples from seeds 900000-900049
>
> Also: A100 GPU for training (was L4), eval seeds declared in OVERVIEW.md.

## Files introduced / modified

| Path | Change |
|---|---|
| `lem/narrate/batch.py` | New, 245 LOC. Batch [[narration]] generator: N seeds × 10 decls × 4 perspectives in one pass; filters to trick-6 decisions (`|legal|>=2`, E[Q] gap >= threshold); outputs JSONL with full grading data (`bot_eq`, `best_eq`, `all_eq`, `legal_actions`); eval seed protection |
| `lem/gemma_star/train_stage0.py` | Modified: GPU switched from L4 to A100 |
| `lem/OVERVIEW.md` | Modified: new "Held-out eval seeds" section (+10 lines) |

## New OVERVIEW section (verbatim)

> ## Held-out eval seeds
>
> Seeds 900000–909999 are **permanently reserved for evaluation**. They must never appear
> in any training dataset. All E[Q] delta measurements use this range. This is declared
> here so it's never ambiguous.
>
> Training seeds: 0–899999 (routed by seed % 1000 < 950 for train, >= 950 for val within
> training data). Eval seeds: 900000–909999 (never trained on, never validated on — pure
> held-out).

## Key claims

- 200 seeds × 10 decls × 4 perspectives processed in 8.5 min on 3050 Ti
- Yield: 3148 train examples (seeds 0–199), 812 eval examples (seeds 900000–900049)
- Filter criteria: trick-6 decisions, `|legal| >= 2`, E[Q] gap >= threshold
- JSONL output carries full grading data needed for [[k1-grading]]: `bot_eq`, `best_eq`, `all_eq`, `legal_actions`
- Eval seed range (900000–909999) protected by default; `--allow-eval-seats` required to override
- Training script GPU upgraded from L4 to A100 for Stage 1 training runs

## Related wiki pages

[[lem]] · [[narration]] · [[decisions/eval-seed-holdout]] · [[modal]] · [[k1-grading]] · [[expected-q-value]] · [[star]] · [[sources/24ae55a]]
