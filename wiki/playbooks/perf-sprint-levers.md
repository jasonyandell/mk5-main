---
title: Perf Sprint — Lever Ladder
kind: playbook
first_seen: fbe798f
last_updated: forge (iter 20 lever #6 cohort abstraction MVP CONFIRMED ACTIVE: --cohort-size flag + run_bench_continuous_cohorts wrapper land bf16 batch=8 cont N=100 cleanly with cohort_size=50; cohort-1 paired gate K1=92% regret=-9.58% wall +3.7%; both cohorts emit no jetsam; first viable workaround for the iter 17/18 cumulative-pressure ceiling on M5 Max)
status: active
---

Levers ordered by expected ROI for an M5-Max edge-class Burl-style workload. **Suggestions, not procedure** — the loop ([[perf-sprint-loop]]) decides keep/discard on `wall_s_per_decision`. This page is hypothesis fuel: read it for ideas to seed the next iteration, freelance is fine. Append wins, retire dead ones, evolve the ladder.

Lever notes age. mlx-lm releases ship frequently and "untested" or "could be already-applied" notes are stale by default — web-search the upstream changelog or model card before assuming the note is current.

## Active ladder (try in order)

| # | Lever | Notes |
|---|---|---|
| 1 | `--batch` sweep: 5 → 8 → 10 → 12 → 16 at PLE-safe quant | CLOSED for current PLE-safe options. Q4 (UD-MLX-4bit) and Q8 (FakeRockert543) BOTH flip gi=0 deterministically at temp=0 (Q4 in iters 2/4/5, Q8 in iter 6 — same play=6 / regret 7.632); Q8 also adds gi=104 damage (play=19 vs bf16 play=6, regret 0.025). Wall improvement is real (Q4 ~42–44%, Q8 ~32%) but the gate is structurally unreachable on this subset for any current PLE-safe quant. Pre-condition for re-opening: a different PLE-safe quant set (different recipe), or a re-frozen subset that excludes gi=0/gi=104, or a widened gate definition. |
| 2 | Q4 PLE-safe Unsloth UD + continuous + max-batch stack | CLOSED. Sprint 2 iter 5 (commit `dac9d28`, reset) ran the bisect: re-ran Q4 UD-MLX-4bit at batch=5 temp=0 (matching the bf16 baseline's batch width). gi=0 STILL deterministically flipped to play=6 with byte-identical regret 7.632 and 60% K1_match — same numbers as iter 4's batch=8 run. Verdict: **pure quant damage**, not batch-prefill-numerics. UD-MLX-4bit's logits on gi=0 prefer play=6 to play=2 regardless of batch width. The 42–44% wall win + ~50% peak-mem reduction is real but the gate is structurally unreachable on this quant. Pre-condition for re-opening: a different PLE-safe Q4 set (different quantization recipe), or a re-frozen subset that doesn't include gi=0-style logit-cliff decisions, or a widened gate definition. |
| 3 | mlx-lm version bump | CLOSED at 0.31.3. Sprint 2 iter 7 (2026-04-28) bumped to mlx-lm 0.31.3 — the latest release ships [PR #1141](https://github.com/ml-explore/mlx-lm/pull/1141) (BatchKVCache extend dim-mismatch fix, the upstream fix for [#1139](https://github.com/ml-explore/mlx-lm/issues/1139)) AND [PR #1158](https://github.com/ml-explore/mlx-lm/pull/1158) (Gemma 4 KV-shared layers fix). #1158 is the blocker: it removes 60 unused k_proj/v_proj/k_norm parameters from the Gemma 4 model class (layers 15-34 are KV-shared per `num_kv_shared_layers=20`), but the official `google/gemma-4-E2B-it` safetensors STILL ship those weights. `mlx_lm.utils.load()` calls `load_weights(strict=True)` and rejects them with `ValueError: Received 60 parameters not in model`. The bench cannot load bf16 Gemma 4 E2B at 0.31.3 at all. Reset on iter 7; restored 0.31.2. **Sprint 2 iter 8 (2026-04-28, commit `9be36dc`, reset)** vendored ~14 LoC `strict=False` loader in `gemma_local_batched.py` (mirrors `mlx_lm.utils.load()`: `_download` → `load_model(strict=False)` → `load_tokenizer`) + re-bumped to 0.31.3. **Vendored loader works** — bf16 model loads cleanly. BUT 0.31.3 carries a hard bf16 regression on this subset: (1) wall jumped from ~38–48s floor at 0.31.2 to ~75s steady-state at 0.31.3 (~70% slower); decode dropped from ~142 tok/s to ~75 tok/s. ps clean, no contention. (2) bf16 at temp=0 is no longer deterministic across runs at 0.31.3 — three back-to-back paired runs A/B/C produced gi=72 play={13, 1, 13}, where 0.31.2 was byte-identical across batch widths (iter5 verified). Sanity gate (must match prior bc4fbd9 numbers + K1=100%) failed on both axes; experimental continuous-batching test skipped because the new baseline is structurally worse and the comparison-anchor is broken. Reset to bc4fbd9; restored mlx-lm==0.31.2. New pre-conditions for re-opening: (a) Google re-issues Gemma 4 safetensors without the unused KV-shared weights AND a fast 0.31.3+ release fixes the bf16 perf/determinism regression, OR (b) wait for 0.31.4+ and re-run iter8's exact vendored-loader+continuous protocol. The vendored-loader recipe itself is sound and reusable when the upstream regression is fixed — see [[perf-sprint-traps]] for the recipe. **Iter 19 update (2026-04-28, ~6 days after 0.31.3 shipped)**: web-fetched github.com/ml-explore/mlx-lm/releases, /tags, /commits/main, and pypi.org/project/mlx-lm — **all three sources show v0.31.3 (2026-04-22) as the latest**, no 0.31.4+ release, no new commits to main past 2026-04-22's #1090 thread-local-stream merge. Lever #3 stays CLOSED on the same pre-conditions. Re-run cadence for the availability check should follow the historical mlx-lm release rhythm (0.31.0 → 0.31.1 was 4 days, 0.31.1 → 0.31.2 was 27 days, 0.31.2 → 0.31.3 was 15 days — ~weekly to monthly cadence; no sooner than ~7 days from 2026-04-28 to avoid spinning on a static target). |
| 4 | `mx.compile` audit on inference hot path | CLOSED. Sprint 2 iter 9 (2026-04-28, audit-only, no code change) inspected mlx-lm 0.31.2's hot path on Gemma 4 E2B. **Already compiled**: (a) sampler chain — `categorical_sampling`, `apply_top_k`, `apply_top_p`, `apply_min_p`, `apply_xtc` all `@partial(mx.compile, inputs=mx.random.state, outputs=mx.random.state)` in `mlx_lm/sample_utils.py:129/154/204/240/277`; (b) Gemma 4 fast paths — `logit_softcap`, `_complete_square`, `geglu` all `@partial(mx.compile, shapeless=True)` in `mlx_lm/models/gemma4_text.py:84/89/94`; (c) every RMSNorm runs through `mx.fast.rms_norm` (Metal kernel-fused, bypasses Python compile graph entirely); (d) attention runs through `scaled_dot_product_attention` (fused MLX kernel). **Not compiled** but unviable: `Attention.__call__` and `DecoderLayer.__call__` (per-token forward) are not `@mx.compile`-wrapped, but they take optional kwargs, conditional branches on `shared_kv` / `per_layer_input`, and variable B/L shapes per call — wrapping near-certain to no-op (real cost is already on the fused kernels) or regress (shape-recompilation churn). `BatchKVCache.update_and_fetch` is plain `mx.concatenate` + index ops (single-kernel ops; compile won't fuse across the boundary). `BatchGenerator._step` (per-token sampling/scheduling) is Python control flow with list comprehensions and variable-length `mx.concatenate` — not compileable as written. Web search (mlx-lm releases through 0.31.3, GitHub blame on `generate.py` and `sample_utils.py`) shows no in-flight `mx.compile` additions on the hot path. Pre-condition for re-opening: a future mlx-lm release (0.32+) restructures `BatchGenerator._step` into a compile-friendly form, OR Apple ships an `mx.compile`-friendly batched `update_and_fetch` for `BatchKVCache`, OR a Gemma 4 model class refactor stabilizes `Attention.__call__` shape signature so `shapeless=True` compile becomes safe. |
| 5 | **Continuous batching @ temp=0 (bf16, batch=5, KEPT)** | **CONFIRMED ACTIVE.** Sprint 2 iter 11 (2026-04-28, no code change — pure variant test of existing `--continuous` flag at perf/aggressive HEAD `8313b7d`) ran paired bf16 batch=5 temp=0 sync-wave vs continuous on `perf_subset_5`. **Wall: sync-wave 67.7s → continuous 41.0s = 39.4% reduction**, matching iter 10's 30–50% prediction. Per-decision walls show the structural win: sync-wave `[67.7, 67.7, 67.7, 60.6, 67.7]` (every decision pays the slowest's cost — straggler-collapse confirmed end-to-end), continuous `[14.6, 24.4, 23.0, 37.7, 41.0]` (each decision finishes when its own turns finish; fast streams free GPU slots for tail streams). Manual K1+regret (silent-anchor trap dodged): K1_match=80% (4/5 byte-identical: gi=0 play=2, gi=36 play=1, gi=104 play=6, gi=136 play=0 — only gi=72 differs); the "regret_delta=-500%" gate breach is **strictly favorable** (baseline_total_regret 1.7338 → variant 0.0000 — the continuous run landed on a better mode of a known bf16-bimodal decision: gi=72 sync-wave history at the same config flips between play=1 and play=13 across runs, see iter5-pre/iter6-pre/iter10-pre, so this is bf16 sampling overlap, not continuous-batching logit damage). Peak-mem 17% lower (10.37GB vs 12.52GB) because continuous never holds 5 streams' KV simultaneously at peak — fast finishers tear down before slow finishers peak. The aggregate decode_tok_s reported by the bench (54 tok/s) is misleading on continuous — the BatchStats `generation_time` accumulates idle scheduler waits between `insert/next_generated` cycles, so the metric undercounts. Per-decision wall is the right metric on continuous and shows the win cleanly. Pre-condition for re-opening as a "batch sweep AT continuous" sub-lever (next iter): bf16 continuous at batch=8/10 — the 7GB peak-mem headroom on the M5 Max should admit batch=8 continuous (estimated ~14–15GB peak); each extra slot in the dispatcher pool reclaims more straggler-tail wall. |
| 5b | ~~Batch sweep AT continuous (bf16 batch=8 continuous, temp=0)~~ | **CLOSED — STRUCTURALLY VOID AT N=5.** Sprint 2 iter 12 (2026-04-28, no code change, pure variant test on commit `8313b7d`) ran paired bf16 temp=0 continuous batch=5 (39.3s) vs batch=8 (51.6s). **Wall REGRESSED 31%.** Per-decision walls: baseline `[17.9, 39.3, 19.2, 36.6, 32.9]` vs variant `[24.2, 39.9, 21.4, 45.2, 51.5]` — every decision got *slower* under batch=8. The hypothesis was structurally wrong on a 5-decision subset: at N=5 with `completion_batch_size=8`, the dispatcher pool has 3 permanently-empty slots (no extra streams ever pending to fill them). Empty slots don't deliver straggler-tail amortization — they just pay scheduler overhead (BatchKVCache allocations sized for 8, prefill padding to width=8, attention masks shaped for 8). For sub-lever 5b to deliver any wall savings, **N must be >> batch_width**, or there's no extra work in the queue to schedule into freed slots. The "widen the dispatcher pool" framing in iter 11's note was correct in mechanism but inverted in scale — adding pool slots beyond the wave size doesn't reclaim straggler tail, it just oversizes the scheduler. Also: peak-mem 17.5% higher (12.22GB vs 10.40GB) because BatchKVCache state is sized for the full pool. **Bonus damage**: gi=104 deterministically flipped bf16's play=6 → variant play=19 (regret 0.0252) at the same temp=0 batch=8 continuous run — the only difference vs baseline was `completion_batch_size`, so this is numerics-shift damage from prefill-padding to width=8 on the BatchKVCache cache.update_and_fetch path. K1_match=80% (gate barely passes), regret_delta=+inf% (variant strictly worse, not bimodal-favorable), wall regressed: discard on every axis. Pre-condition for re-opening at N=5: none — the lever is void at this subset width by construction. The lever ladder pivots to sub-lever 5c (continuous at the 560-decision subset where N >> batch_width is true). |
| 5c | **Pool-scaling at N >> batch (bf16 batch=8 continuous, temp=0, subset_560[:50], KEPT)** | **CONFIRMED ACTIVE.** Sprint 2 iter 14 (2026-04-28, commit `b3f68e0`, kept on perf/aggressive) added `--subset-limit N` flag to slice subset_560 down to N=50 (~14 LoC, mechanical truncation post-row-resolution). Paired bf16 temp=0 continuous on subset_560[:50]: BASELINE batch=5 wall=883.7s per_dec=17.67s decode=135.8 tok/s peak=13.08GB; VARIANT batch=8 wall=664.2s per_dec=13.28s decode=183.5 tok/s peak=13.51GB. **Wall reduction 24.8%** per-decision (inside the 25-40% prediction band, lower end). Per-decision finish-time spread: variant first=478.7s last=664.2s (spread 186s) vs baseline first=658.3s last=875.9s (spread 218s) — variant's tighter spread + earlier first-finish confirms the structural mechanism (dispatcher pool stays full as decisions complete; freed slots fill with new prefills). Real aggregate decode_tok_s = gen_tokens / wall: 135.8 → 183.5 = +35% throughput. The teammate-predicted 250+ tok/s aggregate didn't land at N=50 — at this depth the dispatcher is mostly full but not saturated; sub-lever 5d should push closer. Manual K1+regret across 50 paired decisions: play_match=45/50=90.0%, k1_pass_match=47/50=94.0%, baseline_total_regret=222.012 (mean 4.44), variant_total_regret=203.711 (mean 4.07), regret_delta=-8.2% (variant strictly favorable, within ±10% gate). 5 final_play flips (gi=1: 14→21 regret -19.65, gi=3: 11→16 +0.68, gi=6: 0→27 -9.25, gi=7: 25→19 -7.29, gi=48: 25→15 +17.22). 4 of 5 flips improved regret; only gi=48 worsened materially — net favorable. Peak-mem only +3.3% (vs iter12's +17.5% at N=5) because at N=50 the BatchKVCache amortization dominates over the extra-stream-slot overhead. **Confirms lever 5b's "void at N=5" was a queue-depth issue, not a width issue — pool-scaling really does deliver when N >> batch_width holds.** NOTE on subset_560 first-time observations: at temp=0 bf16, gi=0 plays 25 (not play=2 as on subset_5) — different harness path through `gus_eval_bridge` in subset_560 mode produces different prompts/state vs the `freeze_perf_subset.py` path; not a regression for THIS iter (paired comparison is internally consistent), but flagged for future reference if a subset_560 freeze step gets formalized. Pre-condition for re-opening as sub-lever 5d (next iter): bf16 batch=12/16/24 continuous at N=50 — the dispatcher pool remains under-filled vs queue depth even at batch=8 (decode_tok_s 183 < 250 hypothesis-line); each extra slot continues to reclaim straggler tail until pool ≈ queue or memory caps out (M5 Max comfortably budgets to ~25GB for this workload). |
| 5d | ~~Wider-pool continuous at batch=12 (N=50)~~ | **CLOSED — GATE FAIL AT BATCH=12.** Sprint 2 iter 15-redo (2026-04-28, commit `8deaba4`, perf/aggressive). Paired bf16 temp=0 continuous on subset_560[:50]: BASELINE batch=8 wall=712.5s per_dec=14.25s decode=176.3 tok/s peak=13.91GB; VARIANT batch=12 wall=593.6s per_dec=11.87s decode=212.8 tok/s peak=14.84GB. Wall reduction 16.7% per-decision (inside 10-15% target lower band). **K1_match=86%** (gate PASS), but **regret_delta=+28.8%** (gate FAIL ±10%, and strictly worse direction). 12 final_play flips (vs iter 14's 5 at batch=8): 4 favorable (-24.2 regret), 8 unfavorable (+62.2 regret). Top damage: gi=1 (+19.65), gi=38 (+15.92), gi=10 (+10.29), gi=9 (+9.07). Crossing batch=8 → batch=12 widened the flip footprint 2.4× and reversed the net regret sign. Decode throughput +21% (176 → 213 tok/s real aggregate) — the wider pool is reclaiming straggler tail as predicted, but the prefill-padding to width=12 + BatchKVCache shape changes perturb decode argmax on logit-fragile decisions (subset_560 has more such decisions than subset_5). At batch=12 the perturbations land mostly on the wrong side of argmax. Memory peaked 14.84GB (only +6.7% over batch=8) — well within M5 Max budget; the constraint here is **equivalence, not memory**. Pre-condition for re-opening at batch=12: a widened gate that tolerates flip-set perturbations (e.g. average regret over N≥3 reseeded paired runs at the same batch width to characterize the flip distribution before declaring damage), or a re-frozen subset that excludes logit-cliff decisions. Otherwise the lever is closed at batch=12. |
| 5e | ~~Bisect to largest regret-stable batch (bf16 batch=10 continuous, temp=0, N=50)~~ | **CLOSED — GATE FAIL AT BATCH=10.** Sprint 2 iter 16 (2026-04-28, commit `8deaba4`, perf/aggressive, no code change). Paired bf16 temp=0 continuous on subset_560[:50]: BASELINE batch=8 wall=689.8s per_dec=13.80s decode=177.6 tok/s peak=13.74GB; VARIANT batch=10 wall=605.7s per_dec=12.11s decode=204.2 tok/s peak=14.24GB. Wall reduction 12.2% per-decision (interpolated cleanly between batch=8's 0% and batch=12's 16.7% baseline). **K1_match=84%** (gate PASS), but **regret_delta=+27.7%** (gate FAIL ±10%, strictly worse). 12 final_play flips: 4 favorable (gi=8,10,16,39 net -12.7), 8 unfavorable (gi=2,6,7,9,35,38,41,47 net +54.3). Top damage: gi=38 (+15.92, same logit-fragile decision flipped at batch=12), gi=35 (+10.44), gi=9 (+9.07), gi=6 (+9.25). Decisive finding: at batch=10 the regret-delta is already 96% of batch=12's +28.8% — there is virtually no headroom in the regret distribution between batch=8 and batch=10. **The batch=8 → batch=10 step ALONE doubles the flip count (5 → 12) and reverses the regret sign (-8.2% favorable → +27.7% worse).** The batch=8 → batch=12 favorable region in iter 14 was a sharp threshold, not a smooth gradient — it collapses immediately at batch=9 or batch=10. Decode_tok_s 177.6 → 204.2 (+15.0% throughput, smoothly tracks pool width). Peak-mem +3.6%. **Sub-lever 5d/5e CLOSED at N=50; iter 14's batch=8 confirmed as the regret-stable ceiling.** Pre-condition for re-opening: same as 5d — a widened gate or a re-frozen subset that excludes logit-cliff decisions. The "bisect" hypothesis is now exhausted at N=50 — what remains is to test whether deeper queue depth (N=560 full vs N=50) re-stabilizes the regret distribution at batch>=10, which is sub-lever 5f below. |
| 5f | ~~Pool-scaling at N>50 (bf16 batch=5/8 continuous, temp=0)~~ | **CLOSED — N=50 CEILING DRIFTS DOWNWARD WITH CUMULATIVE SYSTEM STATE.** Sprint 2 iter 17 (2026-04-28, commit `4263721` on perf/aggressive — net no code change vs iter 14's `b3f68e0`; 9c490a7 queue-cap intermediate was reverted as ineffective). Tried to scale iter 14's batch=5/8 cont KEEP from N=50 to N=100, then bisected down: N=100, N=80, N=64, N=56, N=52 ALL silent-died with the same fingerprint — log freezes at `[bench] out_dir=`, all N decision dirs created with only the meta event in events.jsonl, no traceback, no faulthandler output, `ps` shows no python after death. Cause is unified-memory / OS-jetsam: at N>=52 with batch=5 continuous, the peak transient KV+heap exceeds Apple's 40.2GB recommended Metal working-set on the M5 Max (default MLX memory limit is `mx.set_memory_limit` ≈ 49GB = full system RAM, but macOS reclaims aggressively when system pressure rises and SIGKILLs python without raising a Python signal). N=50 is confirmed working (iter 14 KEEP); N=52 is the smallest tested failure → iter 14's N=50 is the operational ceiling for this batch+config on this hardware. Per-iter wall_s for the discard rows is 0.0 (no decisions completed). The "logit-fragility dilutes at full N" hypothesis from iter 16 is now **untestable** at the bench's current scale on M5 Max — the workload doesn't fit. Pre-condition for re-opening: (a) bench refactor that throttles BatchGenerator prefill backlog to keep peak working-set under 40GB (e.g. cap pool to predicted-peak-safe size with backpressure on submit), OR (b) hardware with more unified memory budget (e.g. M5 Ultra), OR (c) cohort abstraction (lever 6) that splits the run into N≤50 cohorts without re-paying model load cost between them. See [[perf-sprint-traps]] for the silent-SIGKILL recipe. **Iter 18 update (2026-04-28, commit `ffc9ef1`):** Two N=50 baseline-only attempts (no scale variant) on the IDENTICAL setup that iter 14 ran cleanly ~9h earlier both silent-died with the same fingerprint (50/50 decision dirs created with only meta event, no python in ps after). System memory was healthy at attempt time (~15GB free, ~12GB inactive of 48GB total), so this is *cumulative* state drift over a sprint session — not acute pressure. The N=50 ceiling is not durable; treat it as a fresh-boot-only claim. ALSO: iter 18 attempted to ship `mx.set_memory_limit(40 * 1024**3)` at bench main() entry as the recipe-(b) guard, then smoke-tested it and **found the limit is a soft guideline, not a hard fence** — `mx.set_memory_limit(2 * 1024**3)` followed by a 3 GB bf16 alloc completed silently. MLX docstring confirms: exception only when limit exceeded AND host RAM+swap is also exhausted, which jetsam SIGKILL preempts. Recipe (b) is wrong premise; the patch ships on perf/aggressive as a no-op marker (cheap to remove). The trap recipe in [[perf-sprint-traps]] has been corrected accordingly. |
| 6 | Spec-decode self-speculation (Q4 draft, bf16 verifier, single-stream lateral) | Doesn't stack with continuous batching but useful for the single-decision path. Was lever #5 pre-iter 10. |
| 6 | **Cohort abstraction implementation (MVP, KEPT)** | **CONFIRMED ACTIVE.** Sprint 2 iter 20 (2026-04-28, perf/aggressive). Added `--cohort-size N` CLI flag + `run_bench_continuous_cohorts` wrapper in `burl/eval/bench_decision_latency.py`: splits N decisions into chunks of size `cohort_size`, calls `run_bench_continuous` per chunk; between cohorts forces `gc.collect()` + `mx.metal.clear_cache()` + `mx.metal.reset_peak_memory()` (BatchGenerator + KV cache already torn down at `gen.close()` inside `run_bench_continuous`). Paired bench at iter 14 setup: BASELINE bf16 batch=8 cont N=50 no cohorts wall=666.96s = 13.34s/dec decode=4.3 tok/s peak=13.86GB (clean iter 14 repro on this hardware-state, dispels iter 18's "ceiling drifts downward" worry for THIS session); VARIANT bf16 batch=8 cont N=100 `--cohort-size 50` wall=1379.3s overall = 13.79s/dec, cohort_walls=[682.4s, 696.7s], decode=4.5 tok/s peak=13.89GB. **Both cohorts ran cleanly through to ledger emission, no silent jetsam — even at total wall ~23min into the second cohort.** Cohort-1 paired (gi=0..49 vs baseline) gate verdict: **K1=92.0% (PASS ≥60%), regret_delta=−9.58% (PASS within ±10%, strictly favorable), wall_ratio var/base=1.037 (PASS ≤1.20×, +3.7% regression).** Inter-cohort overhead is ~2% of total wall (gc + clear_cache + brief BatchGenerator re-warmup); peak_mem near-identical to single-session baseline (13.89 vs 13.86 GB) because each cohort starts with cleared metal cache. Cohort-2 no-baseline (gi=50..99): K1-pass rate (absolute) 66.0%, mean regret 1.999 — first time these decisions have run through the bench at full scale. **The cumulative-pressure ceiling from iter 17/18 was the single-long-running-session model, and tearing down the BatchGenerator + clearing metal cache between cohorts sidesteps it.** Pre-condition for re-opening (next iter): scale to N=200 (4 cohorts of 50) or N=560 (12 cohorts of 50) at same batch=8 cohort_size=50 — confirms cumulative pressure stays bounded across more cohort boundaries. Risk: if "cumulative state drift" (iter 18 finding) is per-process rather than per-session, in-process cohort teardown won't help past some N-cohorts threshold; mitigation = subprocess-isolated cohorts (fork per cohort). See [[perf-sprint-traps]] for the silent-SIGKILL recipe; the cohort wrapper is the FIRST workaround that actually evades it on M5 Max bf16. |
| 7 | ~~Direct mlx Metal kernel audit~~ | **CLOSED.** Sprint 2 iter 13 (2026-04-28, commit `e9f1e6c`, kept on perf/aggressive) shipped a `KernelCounter` + `install_kernel_hooks` instrumentation patch (lives in `bench_decision_latency.py`). Hooks monkey-patch `mx.fast.rms_norm` + `scaled_dot_product_attention` symbols inside `mlx_lm.models.gemma4_text`, `BatchKVCache.update_and_fetch` on the cache class, and `nn.Linear.__call__` at the class level (instance-level `__call__` override is silent no-op in Python — discovered + worked-around) with per-instance `_audit_kernel_name` tags. Decode is bandwidth-bound at B=5 L=1 so per-call wall ≈ weight-tile-bytes / achieved-bandwidth (~400 GB/s on M5 Max bf16 gemv). 5-row breakdown over 9012 gen tokens on bf16 batch=5 continuous temp=0: **matmul_mlp (gate+up+down)** 3×45045 calls @ 18.87MB/call → ~65-70% of decode wall, on Apple MPS bf16 gemv (vendor-fused, no further surface); **matmul_lm_head** (un-measured by audit due to instance-attr path mismatch but architecturally largest single tile/token) ~16-25% on Apple MPS bf16 gemv (vocab=262K, reducing requires retraining); **matmul_qkvo** 4×45045 calls @ 6.29MB/call ~12% on Apple MPS bf16 gemv; **scaled_dot_product_attention_fused** 45045 calls ~4% on `mx.fast.scaled_dot_product_attention` (already fused MLX kernel); **rms_norm_fused** 311454 calls ~<2% on `mx.fast.rms_norm` (already fused). Verdict: every kernel class >1% of decode wall is either already on a fused MLX kernel path (rms_norm, sdpa) or an Apple MPS bf16 gemv call which IS the lowest layer accessible from MLX for bf16 matmul. The only non-vendor-fused GPU work is `BatchKVCache.update_and_fetch` (concat + index_assign, <0.1% of wall) and Python control flow in `BatchGenerator._step` (no GPU time at all). **Pre-condition for re-opening:** (a) Apple ships an MLX-callable triple-fused MLP-gemv kernel (vendor-side change), OR (b) a quant set is found that passes the gate (releases bandwidth proportionally), OR (c) a smaller model variant retains task quality (vocab and hidden_size shrink). Note: Apple Metal capture (`mx.metal.start_capture`) on a 5-decision continuous run produced a 5GB+ .gputrace at 6+min and was killed — capture serializes every command buffer; structurally too heavy for whole-bench capture. See [[perf-sprint-traps]]. |
| 8 | **Prefill prefix-sharing (PROPOSED)** | Reuse the shared system+user+rules prompt prefix's KV cache across decisions in a wave/cohort instead of re-prefilling per decision. Mechanism: hand `BatchGenerator` a prepopulated cache rather than re-running prefill on the shared 1500–2500 tokens. Where: `burl/eval/bench_decision_latency.py` `_render_prompt_ids` + the BatchGenerator construction site; check mlx-lm 0.31.2's `prefill_step_size` and whether `generate_step` / `BatchGenerator` accepts an externally-built cache (web-verify the surface — this matured in 0.31.x). ROI estimate: prefill = 5.79% of wall (iter 10 fingerprint); shared prefix ≈ 70% of prefill text → best case ~3–5% wall reduction at current bf16 batch=8 continuous floor. **Re-opens** sprint 1's "LRU prompt cache" closure under its stated pre-condition (prefix-only at wave-start, never bridging assistant turns). Risk: chat-template alignment bug from sprint 1's first attempt — verify the cache state at wave start byte-matches what mlx-lm produces from a fresh prefill before trusting any wins (paired-protocol works for this; if K1 drops or regret_delta drifts, the cache is misaligned). Cheap, low gate-risk (no model/quant change). |
| 9 | **CPU/GPU pipeline overlap audit (PROPOSED, research-only first iter)** | iter 10's `PhaseTimer` profiled the GPU phases only. CPU-bound work (tokenizer.decode of generated ids, JSON serialization to events.jsonl, per-decision parsing of model output, gus tool dispatch readiness) may run serially with GPU decode where it could overlap. Mechanism: identify sync points where CPU work blocks the next GPU step. Where: `BatchGenerator._step`, the per-step work between `next_generated()` calls in the bench, the per-decision wrap-up after each stream completes. ROI estimate: 5–10% wall if sync points exist; possibly 0% if mlx-lm already streams correctly. Tooling: macOS `Instruments.app` (Time Profiler + System Trace concurrent), or a Python CPU profiler (`py-spy`) running alongside the bench. First iter is research-only (audit + profile pass to identify sync points). If sync points found, second iter ships the overlap. |
| 10 | **mlx-lm parallel-tool-call coupling (PROPOSED, design pass first)** | mlx-lm 0.31.x ships parallel-tool-call handling (PRs [#1170](https://github.com/ml-explore/mlx-lm/pull/1170)/[#1171](https://github.com/ml-explore/mlx-lm/pull/1171); web-verify which release line — iter 7 saw them on 0.31.3 which carries the bf16 regression, so the coupling may need to wait for 0.31.4+ unless the mechanism is reachable from 0.31.2 today). Today the wax_museum gate dispatches one tool call per turn through Python control flow (`apply_step` = 4.78% of wall in iter 10's breakdown × 6 turns/decision ≈ ~28% of wall in sequential control-flow work). Mechanism: push some dispatch through mlx-lm's tool surface so it amortizes alongside generation rather than blocking it. **Hard constraint**: the [[wax-museum]] gate state machine + the model's trained one-tool-call-per-turn rhythm (memory: 297/297 in burl-2000-harvest, 1434/1434 in sprint 1 evidence) cap how much can actually be parallelized — the rhythm is in the SFT, not the harness. ROI estimate: 10–20% wall (some fraction of the 28%; likely smaller than headroom suggests because of the gate constraint). First iter is research-only design pass: map wax_museum gate transitions vs mlx-lm's tool surface insertion points; identify the minimum coupling that delivers a win without breaking gate-state semantics. **Don't open the lever until the design pass surfaces a viable coupling.** |
| 11 | **Burl-aware continuous-batching dispatcher (STRUCTURAL, flag only)** | Custom continuous-batching dispatcher that owns gate transitions, batched tool dispatch across streams, and per-decision state on the hot path — effectively a fork+specialize of `BatchGenerator` for Burl's workload shape. **Multi-week structural project — not a sprint iter.** Belongs as its own sprint with its own design doc, not as a knob the loop picks. Listed here so the option is visible to the playbook's view, not for spawn picks. ROI estimate: 10–20% wall on top of existing wins; combined with #8 + #9 + #10 plausibly pushes the harness toward [[perf-on-the-table]]'s "compounded realistic stack" of 7–10× over the original sprint baseline. Pre-condition for opening: a clean design doc + a fresh sprint dedicated to the rewrite. Sub-levers #8/#9/#10 are the closest reachable approximations within this sprint's iteration cadence. |
| 12 | **`apply_step` sub-phase profiling (PROPOSED, research-only — mirrors iter 10)** | iter 10's `PhaseTimer` found `apply_step` = 4.78% of wall on the sync-wave 68.9s baseline = 3.29s **total across the 5-decision run** (n=6 wave calls × mean 549ms/call; calls are per-wave, not per-decision). **Per decision: ~660ms of apply_step, ~5% of iter 14's 13.28s/dec floor.** Per-stream-per-turn (5 streams × ~5 turns ≈ 26 stream-turns) ≈ **110–140ms per stream-turn** in the harness. Memory says actual tool execution is "sub-millisecond per call (table-cached)" → **~100× gap** between the cached-tool inner loop and what one stream-turn of `apply_step` costs end-to-end. That's where the lever lives — find what's eating the other 99% (chat-template re-rendering is the leading suspect). **Right-sized framing**: this is *not* the biggest harness overhead — decode dominates at ~85–90%, which is mostly Gemma 4 doing actual work. Worth investigating because (a) the per-stream-turn 100× gap suggests fixable structural overhead, and (b) any fix here helps not just the bench but real harvest/eval/training-collection paths. Tool dispatch is per-turn not per-wall-time, so its share of wall *grows* as model-side wall shrinks — every model-side win raises apply_step's relative share. Mechanism: wrap `_apply_step` in `burl/wax_museum/harness.py` (and the bench-side caller in `bench_decision_latency.py`) with a sub-phase timer splitting into: `chat_template_render` (re-rendering accumulated history through Gemma's chat template), `tool_call_inner` (the actual tool function — table-cached should be sub-ms; gus-computed is unknown), `state_update` (gate-state mutation + per-decision-state mutation), `serialize_result` (structured tool result → string for the model to read), `gate_validation` (REJECTED-on-out-of-state checks). Emit per-call breakdown into the per-run JSON. Same diagnostic shape as iter 10's `PhaseTimer`; same downstream effect — the fingerprint points the next lever. ROI: pure diagnostic; the win is unblocking #13. First iter is research-only (instrument + run paired bf16 batch=8 cont temp=0 subset_560[:50]; gate auto-passes because no model-path code change). Likely produces a `keep` row with `wall_s≈baseline` (instrumentation overhead is sub-ms per call) plus a fingerprint table in the description. **Important framing**: tool dispatch is per-turn, not per-wall-time, so this overhead doesn't shrink as the rest of the harness gets faster — every wall_s win on the model side increases tool-dispatch's share of the remaining wall. |
| 13 | **`apply_step` optimization (CONDITIONAL on #12 findings)** | Branches by what #12's fingerprint reveals — pull whichever sub-lever the dominant phase points to. **(a) Incremental chat-template caching** — if `chat_template_render` dominates, cache the rendered prefix tokens and only re-render the new turn's delta. mlx-lm 0.31.x's tokenizer surface has primitives for this; the harness currently re-renders the full history per turn. ROI: most of `chat_template_render`'s share. **(b) gus non-cached path optimization** — if `tool_call_inner` dominates on `belief_trajectory()` / `explore_game()` for novel game states, gus is doing real (non-table-cached) computation. Three sub-paths: extend the table cache to cover the novel states; OR push gus's hot path onto GPU; OR scope a separate gus perf sprint. **Caveat**: optimizing gus is in-scope for *this* perf metric only insofar as it reduces wall_s_per_decision; deeper gus work belongs to its own sprint. **(c) Avoid round-trip serialization for table-cached returns** — if `serialize_result` dominates, structured tool results are being rendered to strings (and possibly re-parsed) on every call. Cache the string form alongside the structured form; lazy-render only on cache miss. **(d) Pre-compute frozen-subset gus calls (BENCH-ONLY)** — if `tool_call_inner` dominates AND the gus computation is deterministic on game state, precompute every `belief_trajectory()` / `explore_game()` for every game state in `perf_subset_5` and `subset_560[:50]` once, persist as a fast lookup table the bench-mode wax_museum reads from. **Important caveat**: this makes the BENCH faster but doesn't help production harvest/eval which see novel states. Useful only as a "what's the model's contribution vs what the harness adds?" measurement separator. Mark any wall_s gain from this sub-lever as `bench-only` in the description; do NOT treat it as a production-translatable win. **Pre-condition for opening any sub-lever**: #12 fingerprint shows the corresponding phase dominates. Don't open all four — pick the one that the diagnostic points at. |

## Wall-time fingerprint (bf16 baseline, sync-wave, batch=5 temp=0, M5 Max, mlx-lm 0.31.2)

Sprint 2 iter 10 (2026-04-28, commit `8313b7d`, kept) instrumented the sync-wave bench with a `PhaseTimer` context manager wrapping seven phase boundaries: `prompt_build_outer`, `prompt_build_inner`, `prefill_decode`, `tokenizer_decode_output`, `init_decision_states`, `apply_step`, `finalize`. Sub-ms cumulative overhead. Paired bf16 batch=5 temp=0 (baseline pre-instrumentation 74.1s, variant 68.9s, plays byte-identical across 5/5 decisions, gate passes cleanly).

Phase breakdown (sorted by % of bench wall):

| Phase | wall_s | % | n | mean ms |
|---|---:|---:|---:|---:|
| `prefill_decode` | 65.52 | **95.12%** | 6 | 10920 |
| `apply_step` | 3.29 | 4.78% | 6 | 549 |
| `prompt_build_inner` | 0.045 | 0.07% | 6 | 7.6 |
| `prompt_build_outer` | 0.022 | 0.03% | 6 | 3.8 |
| `tokenizer_decode_output` | 0.002 | 0.00% | 6 | 0.3 |
| `init_decision_states` | 0.001 | 0.00% | 1 | 0.7 |
| `finalize` | 0.001 | 0.00% | 1 | 0.6 |

Sub-phase split inside `prefill_decode` (from `BatchStats` already collected by the bench):

- prefill GPU time: **3.99s = 5.79% of wall**
- decode GPU time: **61.53s = 89.34% of wall**
- scheduler/sampling overhead: GPU sums match phase wall to <0.01s — essentially zero

**Decisive finding for the lever ladder.** The bf16 baseline workload at temp=0 batch=5 is **pure decode-bound**:

- Decode is 89.34% of wall — token-gen levers were the right targets through iters 1-9; the closed levers covered the right surface area.
- Tool dispatch (`apply_step`) is 4.78% of wall — caching tool results or batching `explore_game()` would save sub-1s. Not worth a lever.
- Prompt build (inner + outer combined) is 67ms — 0.1% of wall. Pre-formatting the system prompt would save microseconds.
- Prefill is 5.79% of wall — a prefill-batching lever (different from `--batch`) is uninteresting at this baseline.

**Per-step straggler collapse in the sync-wave loop.** Per-step decode_tps from the iter 10 variant:

| step | n_active | gen_tokens | gen_time | decode_tps | reading |
|---:|---:|---:|---:|---:|---|
| 1 | 5 | 2918 | 21.93s | **133** | normal batched decode |
| 2 | 5 | 363 | 14.81s | **24.5** | straggler collapse — 1 long stream holds 4 idle |
| 3 | 5 | 639 | 4.26s | **150** | normal |
| 4 | 5 | 525 | 5.32s | **99** | mild straggler |
| 5 | 5 | 372 | 11.09s | **33.6** | straggler collapse |
| 6 | 2 | 359 | 4.12s | **87** | tail (only 2 streams left) |

The bench's reported aggregate decode_tps (84 in iter 10 variant) is a weighted mean dragged down by steps 2 and 5 where the sync-wave forces all 5 streams to march in lockstep. The model's solo decode rate is ~133-150 tok/s at this batch width; the wall isn't the model's solo rate, it's **the marginal cost of one slow stream holding the whole wave**.

**Implication for lever #5 (continuous batching).** The existing `run_bench_continuous` + `--continuous` flag (lever #2 in sprint 1, last run 2026-04-27 commit `c002075` at 34.5s on `perf/batch`) directly attacks this straggler phase: as a stream finishes, its slot fills with the next decision's next turn rather than waiting for the wave. The breakdown predicts **30-50% wall reduction** if continuous batching preserves per-decision plays at temp=0. The sprint 1 continuous-batching numbers were at temp=0.6 where the gate was broken; rerunning at temp=0 against the iter 5 bf16 deterministic baseline is the obvious next iter.

**Iter 11 confirmation (2026-04-28).** Ran the predicted variant — paired bf16 batch=5 temp=0 sync-wave (67.7s) vs continuous (41.0s). **39.4% wall reduction** lands inside the 30–50% prediction band. Per-decision walls show the structural mechanism: sync-wave's `[67.7, 67.7, 67.7, 60.6, 67.7]` collapses into continuous's `[14.6, 24.4, 23.0, 37.7, 41.0]` — the straggler tax is gone. The new bf16 floor at temp=0 batch=5 is **41s, not 67s**, and the lever ladder pivots to "batch sweep AT continuous" (sub-lever 5b).

## Kernel-class fingerprint (bf16 baseline, continuous, batch=5 temp=0, M5 Max, mlx-lm 0.31.2)

Sprint 2 iter 13 (2026-04-28, commit `e9f1e6c`, kept; instrumentation patch on perf/aggressive). The `KernelCounter` audit instrumented every `nn.Linear` projection on Gemma 4 E2B's hot path plus `mx.fast.rms_norm`, `scaled_dot_product_attention`, and `BatchKVCache.update_and_fetch`. Decode is bandwidth-bound at B=5 L=1 — every matmul is a vector-matrix gemv whose wall is dominated by reading the weight tile from unified memory at ~400 GB/s achieved on M5 Max bf16. The audit measures call counts; cumulative time is `(calls × bytes_per_call) / achieved_bw` — analytically the right metric on a bandwidth-bound workload, not requiring an Xcode .gputrace replay (Apple Metal capture was attempted; produced 5GB+ in 6min for one bench run and was killed — see [[perf-sprint-traps]]).

5-row table over 9012 generated tokens on bf16 batch=5 continuous temp=0:

| Kernel class | est cum time | % decode wall | fusion status | optimization surface |
|---|---:|---:|---|---|
| matmul_mlp (gate+up+down) | ~33s | ~65-70% | Apple MPS bf16 gemv (vendor-fused) | NONE — gemv is the lowest layer accessible from MLX for bf16 matmul. Tile-fusion of 3 matmuls would need a custom MLX kernel and triple-fused gemv has no precedent in MLX/MPS. |
| matmul_lm_head | ~9s (un-measured analytical) | ~16-25% | Apple MPS bf16 gemv | NONE — vocab=262144 is structural; reducing requires retraining. The audit's instance-attr path missed this; doesn't change the verdict (still on Apple's MPS-gemv path). |
| matmul_qkvo (attn 4 projections) | ~6s | ~12% | Apple MPS bf16 gemv | NONE. |
| scaled_dot_product_attention_fused | ~2s | ~4% | `mx.fast.scaled_dot_product_attention` (single fused MLX kernel) | NONE — already fused. |
| rms_norm_fused | <1s | <2% | `mx.fast.rms_norm` (single fused MLX kernel) | NONE — already fused. |

The audit prints rms_norm as 39.12% of decode wall; that's a measurement artifact — `bytes_per_call` is captured on the FIRST rms_norm call which lands on a long PREFILL input (B=5 × L=1620 × D=1536 × bf16 ≈ 124 MB), not a decode call (B=5 × L=1 × D=1536 × bf16 ≈ 30 KB). The corrected decode-only rms_norm wall is well under 2% of decode wall, consistent with iter 9's audit finding that rms_norm runs on a single fused Metal kernel.

**Decisive finding for the lever ladder.** Every kernel class >1% of decode wall is either (a) already on a fused MLX kernel path (rms_norm, sdpa) or (b) an Apple MPS bf16 gemv call which IS the lowest layer accessible from MLX. The only non-vendor-fused GPU work is `BatchKVCache.update_and_fetch` (concat + index_assign, <0.1% of wall) and Python control flow in `BatchGenerator._step` (no GPU time at all). **Subset_5 has no sub-fused-kernel ROI.** The decode-side ladder for subset_5 is fully exhausted: levers #1, #2, #3, #4, #5/5b, #7 all closed pending external upstream changes. Sprint floor at iter 11's 41s remains. Strategic question is now: switch to subset_560 and run sub-lever 5c (continuous batch sweep where N >> batch_width is true), pivot to lever #6 cohort abstraction, or accept iter 11 as the sprint floor.

## Closed levers (don't re-try without a pre-condition met)

- **Bench resilience (try/except quarantine + `prefill_batch_size=2`).** Applied in sprint 2 iter 1 (commit `bc4fbd9` on `perf/aggressive`). Sync-wave `_apply_step` is now wrapped; both BatchGenerator constructions pin `prefill_batch_size=2`. Variant ran clean at 38.9s (≈baseline 38.3s, gate passed). Pre-condition for re-opening: a future bench rewrite that re-introduces an unprotected `_apply_step` call site or removes the `prefill_batch_size` pin.
- **Parallel tool dispatch within a turn.** Empirical 1434/1434 one-call-per-turn under [[wax-museum]]. Pre-condition for re-opening: a future Burl SFT round trains a parallel-tool-call rhythm into the model.
- **LRU prompt cache (turn-to-turn KV reuse).** Two structural failures: `_merge_caches` heterogeneous-pad penalty + chat-template alignment bug. Pre-condition: prefix reuse at wave-start (system+user only, never bridging assistant turns).
- **Spec-decode through `BatchGenerator`.** mlx-lm 0.31.2's `speculative_generate_step` doesn't plumb through. Pre-condition: mlx-lm version that exposes batched spec-decode. Note: 0.31.3 changelog does NOT show batched spec-decode plumb-through (verified iter 7); the closure stands.
- **Gemma 3 270M IT as cross-family draft model.** Tokenizer collapses Gemma 4 special tokens (`<|tool_call>`, `<|channel>`, etc) to byte-fallback subword sequences. Pre-condition: a draft model whose tokenizer respects Gemma 4's special tokens.

## PLE quant landmine — known sets

- **Broken (do not use):** `mlx-community/gemma-4-*-{4,8}bit`, `unsloth/gemma-4-*-MLX-{4,8}bit` (the non-UD ones). Quantize per-layer-embedding (PLE) layers, produce garbage outputs.
- **PLE-safe but NOT byte-equivalent:** `unsloth/gemma-4-E2B-it-UD-MLX-4bit`. Sprint 2 iter 4 (2026-04-28) at temp=0 batch=8 deterministically flipped gi=0 from bf16's play=2 (regret 0) to play=6 (regret 7.632) on the 5-row subset. Sprint 2 iter 5 (2026-04-28) closed the open question with the bisect: re-ran at batch=5 temp=0; gi=0 still flips to play=6 with byte-identical regret. **Verdict: pure quant damage on gi=0's logit landscape, not batch-prefill numerics.** Q4 UD-MLX-4bit gives ~42–44% wall win + ~50% peak-mem reduction but the equivalence gate is structurally unreachable on this subset.
- **PLE-safe but NOT byte-equivalent:** `FakeRockert543/gemma-4-e2b-it-MLX-8bit`. Sprint 2 iter 6 (2026-04-28, commit `1e82482`, reset) at temp=0 batch=5 deterministically flipped gi=0 to play=6 with **byte-identical** regret 7.632 to Q4 UD-MLX-4bit's flip — gi=0's logit landscape is fragile to ANY quant noise on this checkpoint, not Q4-specific. Q8 also introduced NEW damage at gi=104 (bf16 play=6 K1=True regret=0 → Q8 play=19 K1=False regret=0.025), giving Q8 a wider damage footprint than Q4 (2 K1 flips vs Q4's 1). Wall win 32% (32.8s vs 47.9s baseline, decode 233 tok/s vs 142 tok/s); peak-mem reduction only ~17% (10.16GB vs 12.34GB) — Q8 keeps most of bf16's footprint, so the batch-headroom rationale Q4 had is mostly absent. The "~16× smaller quant noise" prior did NOT hold on gi=0. Verdict: Q8 PLE-safe is dead for the gate-passing path on this subset.
- **Likely-safe (untested at temp=0):** `FakeRockert543/gemma-4-e2b-it-MLX-4bit` (the non-UD Q4). Lower priority than non-quant levers given Q4 UD + Q8 both failed identically on gi=0; would expect the same flip.

## Append a lever

When a sprint discovers a new viable lever, add a row above with name, expected ROI, and a link to the experiment that proved it. When a lever closes, move it down with the pre-condition for re-opening.

## Links

[[perf-sprint]] [[perf-sprint-traps]] [[perf-sprint-history]] [[perf-on-the-table]]
