---
title: Perf Sprint — Trap Recipes
kind: playbook
first_seen: 2026-04-27
last_updated: 2026-05-03
status: active
---

Known crashes, footguns, and contention modes. When the loop hits one of these, here's the smallest fix — apply it and re-run rather than re-deriving from scratch. Append when a sprint discovers a new failure mode.

Trap recipes age. mlx-lm and Gemma 4 are moving weekly — before spending an iteration on a workaround, web-search the upstream changelog or GitHub issue tracker; the bug may already be fixed.

## Bench crashes

### Silent SIGKILL right after `[bench] out_dir=` print at N>=52 in continuous mode (M5 Max, bf16 batch=5/8)

- **What it is.** Sprint 2 iter 17 (2026-04-28) tried to scale iter 14's batch=5/8 cont KEEP from N=50 to N=100. The bench died silently at every N tested above 50: N=100, N=80, N=64, N=56, N=52 ALL produced the same fingerprint — log freezes at `[bench] out_dir=...`, ALL N decision dirs are created (because `_init_decision_state` ran first and writes a meta event per dir), each `events.jsonl` has exactly 1 line (the meta), no traceback in stdout or stderr, no faulthandler output, `ps` shows no python after death. Pump-loop diagnostic prints showed the bench DOES enter the pump and run ~50 decode steps before dying mid-decode at N=100 with no decision finishing. Cause is unified-memory / OS-jetsam: at N>=52 with batch=5 cont on Gemma 4 E2B bf16, the peak transient KV+heap exceeds Apple's recommended 40.2GB Metal working-set on the M5 Max (queryable via `mx.device_info()['max_recommended_working_set_size']`). MLX's default memory limit is `mx.set_memory_limit` ≈ 49GB = full system RAM, but macOS reclaims aggressively when system pressure rises and **SIGKILLs python without raising any Python-level signal** — bypasses faulthandler entirely. iter 14's N=50 (peak 13.51GB) is the operational ceiling for this batch+config on this hardware; N=52 fails the same way as N=100. Iter 17 first tried a queue-cap fix (commit 9c490a7, capped initial submits to batch*4 instead of all N up-front) on the hypothesis that the up-front submit pile was the bound — that fix was a **no-op for this bug** (death still occurred mid-pump after init+submit completed) and was reverted in commit 4263721.
- **Recipe (workarounds, no clean fix).** (a) Stay at N where peak transient KV+heap < jetsam threshold for the *current* system state — was N=50 for iter 14 on a fresh-boot M5 Max but **iter 18 reproduced the silent-die at N=50 itself** (~9h after iter 14), so the ceiling drifts downward with cumulative process state / fragmentation across a long sprint session (not just acute system pressure — system was healthy at iter 18 attempt: ~15GB free, ~12GB inactive). Treat "N=50 works" as a fresh-boot-only claim. (b) Cohort abstraction (lever 6) — split N into multiple small cohorts and reuse the loaded model + adapters across cohorts to amortize the ~2s setup. This is the only durable workaround on M5 Max bf16. (c) Throttle BatchGenerator prefill backlog to keep peak working-set under 40GB (e.g. cap pool to predicted-peak-safe size with backpressure on submit) — this is what 9c490a7 ATTEMPTED to do on a wrong hypothesis but the queue cap alone doesn't reduce per-stream KV growth during decode. Always launch from the venv python (`/Users/jason/code/mk5-main/.venv/bin/python`); the system python doesn't have torch and dies with `ModuleNotFoundError: No module named 'torch'` in `gus_eval_bridge` import — that's a separate, loud crash, but a useful sanity check.
- **What does NOT work — `mx.set_memory_limit` is a SOFT GUIDELINE.** Sprint 2 iter 18 (2026-04-28, commit `ffc9ef1`) added `mx.set_memory_limit(40 * 1024**3)` at bench `main()` entry, expecting that to convert silent jetsam into a visible `RuntimeError`. **It does not.** Smoke test: `mx.set_memory_limit(2 * 1024**3)` followed by `mx.zeros(shape=(1_500_000_000,), dtype=mx.bfloat16)` (a 3 GB bf16 alloc, 1.5× the limit) — alloc completes silently, no exception. MLX's own docstring is explicit: *"The memory limit is a guideline for the maximum amount of memory to use during graph evaluation. If the memory limit is exceeded **AND there is no more RAM (including swap when available)** allocations will result in an exception."* So the limit only fires when both (a) the soft guideline is exceeded AND (b) host RAM+swap is exhausted. macOS jetsam SIGKILLs the process at the OS level **before** condition (b) holds, so the guard cannot fire. The patch ships on perf/aggressive as a no-op marker (cheap to remove if MLX ever changes the semantics) but does NOT deliver any new error surface. Earlier versions of this recipe claimed otherwise — that claim was wrong.
- **Long-term fix.** Cohort abstraction (lever 6) is the cleanest path — splits the workload into pieces that fit. Alternatively, a hardware bump (e.g. M5 Ultra) raises the recommended working-set ceiling. The `mx.set_memory_limit` guard is **not** a workaround, contrary to earlier recipe text — see the "What does NOT work" callout above.
- **Iter 22 update — ceiling drift confirmed at scale, cohort cleanup does not defeat cumulative session pressure.** Sprint 2 iter 22 (2026-04-28) tried to validate lever #6 at production scale: bf16 batch=8 cont N=560 `--cohort-size 50` → 12 cohorts. Cohort 1/12 silent-died ~70s in with the same fingerprint (process gone from ps, log frozen at `[bench] cohort 1/12: decisions 0..49 (n=50)`, all 560 decision dirs pre-created with only meta events, no traceback). Bisect to N=300 (6 cohorts) ALSO silent-died ~35s into cohort 1/6 with identical fingerprint — well above the iter 18 N=50 ceiling but well below the iter 21 clean N=200 from the same session ~3h earlier. The cohort cleanup (`gc.collect` + `mx.metal.clear_cache` + `mx.metal.reset_peak_memory` between cohorts) IS durable across cohort boundaries (iter 21 monotonically-decreasing per-cohort walls prove it) but does **not** defeat OS-level jetsam pressure that accumulates across run boundaries within a single multi-hour python invocation chain. **Operating envelope** on M5 Max bf16 batch=8 cont with the current bench architecture: fresh-boot ceiling N≥200 (iter 21 confirmed); multi-hour-sprint ceiling N<300 (iter 22). The ceiling drifts downward as the session heats up — treat any "N≤X works" claim as fresh-boot-only. Workaround for production scale: subprocess isolation per cohort (each cohort runs in a fresh python invocation, reusing only the on-disk model weights cache) — needs validation on a fresh-boot system; was not tested in sprint 2 because the heated session would have confounded the result.
- **Iter 23 update — ceiling drift is REVERSIBLE; the system COOLS during inter-iter idle.** Sprint 2 iter 23 (2026-04-28) ran a single bf16 batch=5 cont N=50 as a system-pressure diagnostic, no paired comparison — the goal was to find out where the jetsam ceiling sat after the iter 22 collapse below N=300. Pre-flight `ps aux | grep python | grep -v grep` was clean (only beads-mcp / editor helpers; no zombie ML processes from prior iters). Bench ran to completion: total wall 923.3s, decode 3.13 tok/s, peak 13.19GB, 50/50 decisions, K1 pass rate 60% (30/50, population), mean regret 3.81. Per-decision wall = `bench_wall_s_total / n_decisions` = 18.47s vs iter 14's identical-config (batch=5 cont N=50) baseline of 17.67s on a fresh-boot system — +4.5%, **within the ±4% noise floor** for clean GPU on this hardware (per the perf-subset-5 wall floor finding). Decode 3.13 vs 3.05 tok/s and peak 13.19 vs 13.08GB are both effectively unchanged. The headline finding: **the ceiling is not monotonically declining.** Iter 18 reproduced silent-die at N=50 ~9h after iter 14 ran clean; iter 22 saw silent-die at N=300 then N=560; iter 23 ran clean at N=50 again ~14h into the same sprint with no reboot. The implication: jetsam ceiling drift is driven by something that resets during inter-iteration idle (process exit + GC, OS-level memory compaction, swap eviction, or some combination), NOT a one-way ratchet within a single shell session. **Updated operating envelope**: treat the ceiling as time-varying; a fresh `ps`-clean state + the natural inter-iteration gap (a few minutes between bench launches in an orchestrated sprint) is sufficient for N=50 to recover even after multi-hour pressure has driven the production ceiling below N=300. Subprocess isolation (one fresh python per cohort) is still the cleanest production-N≥300 path because it bounds each cohort to a fresh-process working set, but the iter 23 data point suggests **inter-cohort *idle* (sleep + GC between cohort launches) may be a cheaper zeroth-order workaround** worth measuring in a future sprint before committing to the subprocess-isolation MVP.
- **Iter 24 update — ceiling reversibility CONFIRMED at scale; iter 21's regime fully recovered, iter 20's anchor matched exactly.** Sprint 2 iter 24 (2026-04-28, ~14:06 UTC) ran the iter 23 hypothesis upward: bf16 batch=8 cont N=100 with `--cohort-size 50` (= iter 20/21's confirmed-clean config), single bench, no paired comparison; iter 20's fresh-boot numbers (13.79s/dec, peak 13.89GB) were the implicit anchor. Pre-flight `ps`-clean. **Both cohorts ran to completion with no jetsam, no Tracebacks**: cohort 1 wall 681.9s (13.64s/dec), cohort 2 wall 696.6s (13.93s/dec); aggregate 1378.7s (13.79s/dec — *byte-matches* iter 20's 13.79), peak 13.79GB, decode 4.37 tok/s, K1 pass rate 63/100 (63%), mean regret 2.44. Notably **faster** than iter 21's same-config 15.35s/dec, peak 13.85GB. The ~30min inter-iter gap between iter 22's silent-dies and iters 23/24 was sufficient to fully restore the iter 20/21 working regime — not just the N=50 floor that iter 23 confirmed, but the full N=100 cohort=50 ceiling. **Operating envelope refined**: at N=100 cohort=50 on M5 Max bf16 batch=8 cont, inter-iteration idle (a few minutes of process-exit + GC + OS memory compaction between bench launches) is sufficient as a zeroth-order pressure reset on a heated multi-hour session. Subprocess isolation per cohort is **not** required at this N — the existing `gc.collect` + `mx.metal.clear_cache` + `mx.metal.reset_peak_memory` between cohorts plus the natural inter-iter gap together achieve fresh-boot-equivalent throughput. Subprocess isolation remains the candidate path for N≥300 (where iter 22 saw silent-die during a heated session), but its incremental value over inter-cohort idle is no longer assumed; an N=300 cohort=50 retry on a cooled system should be the next test before committing to the subprocess MVP. Hypothesis next: try N=200 cohort=50 to see how high the cooled system reaches, or jump to N=300 cohort=50 to test whether iter 22's failure was purely heat-driven.
- **Iter 25 update — ceiling reversibility CONFIRMED AT PRODUCTION-RELEVANT N=300; iter 22's failure permanently re-classified as session-pressure transient.** Sprint 2 iter 25 (2026-04-28, launched ~14:36 UTC, ~75min after iter 22's ~13:23 UTC silent-die at the identical config) ran the decisive test: bf16 batch=8 cont N=300 with `--cohort-size 50` → 6 cohorts. Pre-flight `ps`-clean (only beads-mcp / editor helpers, no zombie ML processes). Single bench, no paired comparison; iter 22's silent-die at cohort 1/6 was the implicit failure anchor to refute. **ALL 6 COHORTS RAN CLEAN, no silent-jetsam, no Tracebacks**: total bench_wall=4065.8s, wall_s_per_decision=13.55s/dec (vs iter 20/24 anchor 13.79s/dec — well within ±4% noise floor), decode_tok_s=4.41, peak_mem_gb=14.02 (slight uptick from iter 24's 13.79GB at N=100, consistent with 6 cohorts vs 2), K1 pass rate 204/300 = 68.0% (population, no paired baseline — first time these 300 decisions have ever run through bench at this scale), mean regret 2.209. **Per-cohort walls=[706.1, 634.1, 668.0, 658.7, 677.9, 720.6]** — cohort 1 was the warmup outlier (14.12s/dec, identical pattern to iters 21/24 cold-start), cohorts 2-5 settled in a tight 12.68-13.56s/dec band, cohort 6 ticked up to 14.41s/dec but still well within iter 21's monotonic-decreasing envelope. The 720.6→706.1 cohort-6-vs-cohort-1 ratio (1.02) shows essentially zero session-pressure drift across 6 consecutive cohorts on a cooled system — opposite trend from iter 22's catastrophic-failure session. **Operating envelope refined to its strongest form yet**: at N=300 cohort=50 on M5 Max bf16 batch=8 cont, the existing cohort cleanup primitives (gc.collect + mx.metal.clear_cache + mx.metal.reset_peak_memory) plus inter-iter idle (a few tens of minutes between bench launches) deliver fresh-boot-equivalent throughput across full production-N runs. **Subprocess isolation per cohort is NOT required at N=300.** Iter 22's failure is now permanently re-classified as a session-pressure transient (heat from the prior multi-hour sprint, not a structural ceiling). Hypothesis next iter: scale to N=560 12-cohort (full subset_560) on the same cooled-system / clean-ps preconditions; if it holds, production-N regret comparisons become routine without the subprocess-isolation MVP. If N=560 fails despite the cooled start, the heat budget per cohort is ~50 decisions × N-cohorts before pressure accumulates, and subprocess isolation becomes the marginal MVP for full production scale.
- **Iter 33 update — subprocess isolation MVP works on smoke + cohort 1, but cohort 2 reliably co-jetsams the parent on a heated multi-day-uptime host even after ≥60min cooldown.** Sprint 2 iter 33 (2026-04-28, ~17:25 CDT, no code change vs iter 32 commit `9f52ed0`) re-ran the iter 32 smoke after ~4h idle since iter 32's 13:22 death (well above the iter 31 ≥60min trap floor). Pre-flight `ps`-clean of bench processes, gus adapter symlink intact, system load avg ~46 (heated 9-day uptime). **Smoke at subset_5 1-cohort batch=8 cont temp=0 with `--subprocess-isolation` PASSED CLEAN**: 80.0s wall, 5/5 decisions complete (decision_*/events.jsonl 19-28 events each, real assistant_text + tool_call + tool_result, NOT meta-only), peak 12.6GB, decode 28.9 tok/s. Parent's "skipping model load" verified, child wrote sidecar, parent hydrated and exited 0. The subprocess plumbing is structurally sound on a cooled session. **Paired baseline at subset_560[:50] cohort=25 (2 cohorts) silent-died on cohort 2/2**: cohort 0/2 (decisions 0..24) ran clean in 437.0s wall (17.48s/dec, sidecar `cohort_000_result.json` written). Cohort 1/2 (decisions 25..49) loaded model in 3.0s, entered pump, started 25 streams, ran ~5min of multi-turn decode (decision_25..49/events.jsonl reached 10-16 events each, ts_rel ~30-60s of real progress), then **BOTH parent (PID 13476) AND child (PID 97024) vanished from `ps` simultaneously** with no stderr in run.log; `cohort_001_result.json` never written; events.jsonl mtime frozen at 17:38:12-17:38:38. Variant never ran (baseline crash blocks paired comparison). **Key new finding refining iter 32's recipe**: iter 32 conjectured "subprocess isolation does not bypass jetsam on heated session"; iter 33 narrows that — subprocess isolation **does** survive a single child invocation on a properly-cooled heated host (smoke + cohort 1), but the SECOND child triggers a deterministic group-jetsam sweep of {child, long-lived parent}. Cohort 1's wall (17.48s/dec) matches the iter 30 19ba103 baseline regime, so the plumbing overhead is not the issue. The parent retains accumulated state across cohort_n → cohort_n+1 (file handles, threadlogger, any retained MLX context not cleared by the child's exit, or simply being a same-uid memorystatus peer of a high-Metal-pressure child) that makes the cohort-2 sweep happen even when the child itself is fresh-interpreter. **Updated operating envelope for lever #6 sub-lever a**: subprocess-isolation MVP is valid for smoke / single-cohort. For cohort-N≥2 on a heated host, two paths forward — (a) **parent-as-shell rearchitecture**: outer orchestrator is bash, each cohort = fresh top-level python invocation via `--subset-limit` + `--subset-offset` slicing (the parent never lives long enough to accumulate state); (b) **inter-cohort cooldown**: add `--cool-down-seconds N` to the existing parent so it sleeps ≥60s between `subprocess.run` calls — applies the same ≥60min cooldown principle the iter 25/iter 31/iter 32 recipes recommend at coarser granularity. Path (b) is the smaller change and worth a smoke before committing to (a). **Diagnostic for the new iter 33 fingerprint**: cohort_n_result.json exists for some n<N_cohorts but cohort_{n+1}_result.json never written, decision dirs in cohort_{n+1} have multi-line events.jsonl frozen at the same mtime, and `ps` shows both parent + child gone at the same instant.

- **Iter 32 update — NEW DEATH MODE: mid-decode jetsam with multi-turn progress (subprocess isolation does NOT bypass on a heated session).** Sprint 2 iter 32 (2026-04-28, ~13:22 CDT, commit `9f52ed0` on perf/aggressive) shipped the subprocess-isolation MVP and ran a smoke at subset_5 1-cohort (5 decisions, batch=5, temp=0, continuous). Pre-flight `ps`-clean, gus adapter symlink intact, 36min idle since worker-32's 12:46 crash. Parent's "skipping model load" log line verified — subprocess plumbing was structurally working. Child wrote `out_dir=` at 13:22:43, ran ~19s of pump activity, then **BOTH parent (PID 82041) AND child (PID 83218) vanished simultaneously** from `ps` with no stderr in run.log. **NEW FINGERPRINT — death came mid-decode after multi-turn progress, NOT at init**: decision_0's `events.jsonl` had 8 lines ending in `assistant_text` at ts_rel=18.20s; decision_72's had 16 lines ending in `tool_result` at ts_rel=17.99s; decisions 36/104/136 stayed at 1-line meta. Real generation happened — assistant_text emitted, tool_call dispatched, tool_result returned — then jetsam swept the entire process group. Child's ~10GB working set and parent's ~200MB working set were killed in the same memorystatus sweep (jetsam can target a process group, not just the largest single PID). **Implication for subprocess isolation**: the lever does NOT bypass jetsam on a heated session — the parent gets caught in the same sweep as the killed child, even though the parent never loaded the model. Subprocess isolation's value is for a COOLED system: its job is to prevent cumulative session pressure from accumulating across cohort boundaries, not to make any single child invocation immune to acute jetsam pressure. **Heat root-cause confirmed by iter 31's cooldown rule**: iter 30's calibration burst (4 benches over ~30min) ended 12:28; the ≥60min floor put cooled-launch at ≥13:28; iter 32's smoke launched at 13:22 = 6min short, and worker-32's failed run between 12:34 and 12:46 added more pressure. **Updated recipe**: subprocess isolation is necessary but not sufficient — pre-flight cooldown floor (≥60min after the last bench burst) still applies. **Diagnostic for the new fingerprint**: when a smoke dies mid-pump (per-decision events.jsonl has >1 lines on at least one decision), heat is the most likely cause; the OS killed the process group during a memorystatus sweep that fired on Metal working-set pressure. When ALL decisions stay meta-only (1 line), the death is at init/first-prefill and could be either heat or a structural bug. The two patterns deserve separate diagnoses.

- **Iter 31 update — cooldown threshold lower-bounded; "a few tens of minutes" is too vague.** Sprint 2 iter 31 (2026-04-28, ~12:34 CDT) attempted a paired bench (BASELINE bf16 batch=8 cont temp=0 subset_560[:50] --cohort-size 50) to confirm lever #16 phase 2-redo at wider-N. **Pre-flight gap: only ~14min idle** since iter 30's last bench (paired_variant.log mtime 12:28). System free RAM was healthy at launch (~10.4GB pages free, 25GB active+inactive on 48GB), and `ps -ef | grep python` was clean of bench processes. Despite the apparently-clean preconditions, **baseline silent-died ~5min into pump with iter 17/18/22 fingerprint** (PID vanished, log frozen at `[bench] cohort 1/1: decisions 0..49 (n=50)`, all 50 decision dirs created with 1-line meta-only `events.jsonl`, no traceback). **Refines the operating envelope**: iter 30's preceding work was 4 back-to-back bench passes within ~30min (calibration on subset_5 + calibration on subset_560[:50] + paired baseline + paired variant on subset_5 — collectively ~50min of GPU work). 14min idle is **insufficient cooldown** after this density of bench work. **Empirical cooldown floor for "post-burst recovery"**: ≥60min idle, calibrated against iter 22→iter 25's 75min successful recovery from a single-cohort silent-die. Free-RAM is not a useful proxy for readiness — Metal working-set pressure decays slowly even after process exit, and the silent-jetsam threshold drifts independently of OS-visible memory pressure. **Updated pre-flight check**: before launching any bench after another bench has run within the past hour, **read the mtime of the most recent `iter*/run.log` (or equivalent)** and require at least 60min elapsed; OR fall back to subprocess-isolation-per-cohort which sidesteps cumulative session pressure entirely. The earlier "a few tens of minutes" wording from iter 24/25 understates the requirement after a calibration-burst — those iters had 30min+ post-22 cooldown and ran a single bench, not back-to-back-to-back-to-back.

### `ValueError: [broadcast_shapes] Shapes (4,1,256) and (3,1,1) cannot be broadcast` in `mlx_lm.models.cache.dynamic_roll`

- **What it is.** MLX `dynamic_roll` shape mismatch when batched streams cycle in continuous mode. Cache shape persists from completed-stream count; new input batch has different stream count.
- **Workaround.** Pass `prefill_batch_size=2` to `BatchGenerator` (or whatever lower number works for the current batch size). Or revert that specific run to sync-wave (`run_bench` not `run_bench_continuous`).
- **Long-term fix.** mlx-lm version bump may resolve. Watch for cache-related fixes in changelog. Consider filing an upstream issue with the `(4,1,256)` vs `(3,1,1)` reproducer.

### `ValueError: Received 60 parameters not in model: language_model.model.layers.{15..34}.self_attn.{k,k_norm,v}_proj.weight` at bf16 Gemma 4 E2B load time on mlx-lm 0.31.3+

- **What it is.** mlx-lm 0.31.3 ships [PR #1158](https://github.com/ml-explore/mlx-lm/pull/1158) which removes the `k_proj`/`v_proj`/`k_norm`/`v_norm` modules from Gemma 4 attention layers that are KV-shared (`num_kv_shared_layers=20` for E2B → layers 15-34). The model class no longer has weight slots for those tensors. The official `google/gemma-4-E2B-it` (and `mlx-community/gemma-4-E2B-it-bf16`) safetensors STILL ship them. `mlx_lm.utils.load()` calls `load_weights(strict=True)` and the load aborts with the 60-parameter rejection. Confirmed firing in sprint 2 iter 7 (2026-04-28). bf16 model cannot be loaded at all on 0.31.3 — this is a hard wall, not a degradation.
- **Recipe (vendored loader, ~14 LoC).** Sprint 2 iter 8 (2026-04-28, commit `9be36dc`, reset) verified the vendored-loader path loads bf16 Gemma 4 E2B successfully on 0.31.3. The patch (in `burl/modal/gemma_local_batched.py`):
  ```python
  from mlx_lm import batch_generate  # drop `load` from this import
  from mlx_lm.utils import (
      _download as _mlx_download,
      load_adapters as _mlx_load_adapters,
      load_model as _mlx_load_model,
      load_tokenizer as _mlx_load_tokenizer,
  )

  def _load_strict_false(path_or_hf_repo, adapter_path=None):
      model_path = _mlx_download(path_or_hf_repo)
      model, config = _mlx_load_model(model_path, lazy=False, strict=False)
      if adapter_path is not None:
          model = _mlx_load_adapters(model, adapter_path)
          model.eval()
      tokenizer = _mlx_load_tokenizer(
          model_path, eos_token_ids=config.get("eos_token_id", None),
      )
      return model, tokenizer
  ```
  Then in `__init__`: `self.model, self.tokenizer = _load_strict_false(model_repo, adapter_path=adapter_path)`. Mirrors the four-line body of `mlx_lm.utils.load()` exactly, with the single `strict=False` change. Loader is sound and reusable in any future iter targeting 0.31.3+.
- **Caveat — vendored loader DOES NOT make 0.31.3 usable for the perf gate.** Iter 8 also discovered an mlx-lm 0.31.3 bf16 regression (next trap below) that breaks the sanity gate even with the loader fix. Until that regression clears in 0.31.4+ or upstream, pin `mlx-lm==0.31.2` regardless.
- **Long-term fix.** File an upstream issue requesting `mlx_lm.utils.load()` accept `strict=False` (the underlying `load_model` already does). Or PR upstream a `sanitize` hook for Gemma 4 that drops the unused KV-shared weight names before `load_weights` sees them.

### bf16 Gemma 4 E2B perf + determinism regression on mlx-lm 0.31.3

- **What it is.** Sprint 2 iter 8 (2026-04-28) ran three back-to-back paired bf16 batch=5 temp=0 sanity baselines on mlx-lm 0.31.3 (with the vendored strict=False loader unblocking model load). All three returned per-decision grades that match prior bf16 grades on the deterministic decisions, but two regressions surfaced. **Wall regression**: A=99.3s, B=72.4s, C=73.6s — steady-state ~75s vs the 0.31.2 floor of 38–48s, so ~70% slower. Decode dropped from ~142 tok/s to ~75 tok/s, ps clean, no contention. **Determinism regression**: at the same config (bf16 batch=5 temp=0), runs A and C produced gi=72 play=13 K1=True regret=0; run B produced gi=72 play=1 K1=False regret=1.7338. Iter 5 explicitly verified at mlx-lm 0.31.2 that bf16 at temp=0 was byte-identical across batch widths and runs. **0.31.3 broke deterministic equivalence at temp=0 for bf16 Gemma 4 E2B on this subset.** Either PR #1158's KV-shared cleanup or PR #1141's BatchKVCache rework introduced numerical drift; root cause not bisected.
- **Recipe.** Pin `mlx-lm==0.31.2` until 0.31.4+ ships and the regression is verified clear. Do NOT trust 0.31.3 wall numbers for paired-baseline comparisons even after the strict=False loader fix unblocks model load — both arms of the pair will land at the new structurally-worse floor, so the comparison metric is moot. Re-run the iter 8 sanity protocol (3× bf16 batch=5 temp=0 paired runs, manual cross-comparison of K1+regret) on each future bump to detect when the regression clears.
- **Long-term fix.** File upstream issue with the iter 8 reproducer (3 paired bf16 batch=5 temp=0 runs producing differing gi=72 plays). On a known-good future release, retire this trap and re-open lever #3.

### `AssertionError: assert play is not None` in `burl/wax_museum/schemas.py:next_actions_unchanged`

- **What it is.** A stream completed a turn without calling `explore_game(X)`, leaving `last_explored_play` unset. The bench has no per-decision quarantine path, so the whole run dies.
- **Workaround.** Wrap the `_apply_step` call in `try/except AssertionError`; on failure, mark the decision as quarantined and continue to the next.
- **Long-term fix.** Implement the cohort-as-quarantine-unit pattern (lever #7).

### Default-config (no subprocess, no cohort, no prune) is RSS-clean across the iter32 regression range — only the cohort-N≥2 subprocess path inflates resident set

- **Iter 34 RSS bisect (2026-04-28).** User reported bench hitting >90% RAM and being jetsam'd, suspected regression between `bea9976` (iter 20-25 cohort MVP, last clean at peak 13.94 GB on `subset_560[:50]`) and `9f52ed0` (iter 32 subprocess isolation). Single-bench peak-RSS bisect on `perf_subset_5` batch=5 cont temp=0 bf16, **NO subprocess, NO prune** at all four SHAs (`bea9976` / `ed23d47` / `19ba103` / `9f52ed0`) returned ALL FOUR within ~30 MB of each other: maxRSS 11.13–11.16 GB, MLX peak 12.48–12.60 GB, walls 62.6–71.2s, k1_match 60–80%, regret_delta -12 to -46%. **No regression on the default-config path.** The reported >90%-RAM jetsam is the iter 33 cohort-N≥2 fingerprint (subprocess-isolation parent retains state across `cohort_n → cohort_n+1` and the second child's high-Metal-pressure sweep co-jetsams it), NOT a leak in default config. Lever #16 phase 2 (`ed23d47`/`19ba103`) only allocates the sliced lm_head when `--prune-lm-head` is set, so its dormant code path doesn't move RSS. The 312-line subprocess wiring in `9f52ed0` is gated on `--subprocess-isolation`; the only always-on thing it adds is heartbeat counters in the hot loop (sub-MB).
- **What this means for future bisects.** Don't bisect a peak-RSS regression on default-config when the failing config is `--subprocess-isolation --cohort-size N` with N_cohorts≥2. The default path will be flat across the range. Repro requires the exact failing config; `subset_5 1-cohort + --subprocess-isolation` is the smallest config that exercises subprocess plumbing and passed clean in iter 33, so the floor is `subset_560[:50] cohort=25` (2 cohorts) per the iter 33 trap — but at that scale the run is the failure, not a probe.
- **What this means for `9f52ed0` itself.** The commit is NOT to be reverted. Its subprocess path is already documented as cohort-N≥2-broken (parent-as-shell rearchitecture or `--cool-down-seconds` are the open paths); its always-on contribution is the 30s heartbeat that satisfies the new logging-discipline rule. Reverting would un-satisfy that rule.

## Cross-scribe contention

### Symptom: same-config baseline drifts wildly across runs in one session

- **What it is.** Multiple processes running mlx-lm inference simultaneously on one Apple-Silicon GPU. Metal + unified-memory contention. `ps aux` won't show the contention — the GPU is one shared resource without OS-level visibility.
- **Detection signal.** `decode_tok_s` < 100 = contended; < 60 = heavily contended. On clean GPU at batch=5 bf16 Gemma 4 E2B temp=0, the floor is ~36–40s wall, ~170 decode tok/s, ±4% run-to-run.
- **Recipe.**
  1. `ps aux | grep python | grep -v grep` to find competing processes.
  2. Pause every other inference job until the bench finishes.
  3. Wait 30s for the system to settle.
  4. Re-baseline before continuing.
- **Prevention.** `bench.lock` file convention. Write `bench.lock` before run, delete after; refuse to start if it exists. ~5 lines in the bench. Eliminates detective work.

## Bench protocol footguns

### Silent comparison-anchor mismatch

- **What it is.** The bench's `latest_baseline_run()` selector grabs the latest `baseline-bf16` row in the ledger as the comparison anchor for K1+regret. If the latest baseline was contended, at a different temperature, or from a different code path, the comparison is nonsense. **Confirmed firing in sprint 2 iter 2** (2026-04-28): both the paired baseline (variant_label `baseline-iter2-pre`) and the variant compared against a stale `baseline-bf16` from 20260427_013851 — even though I ran `baseline-iter2-pre` immediately before the variant. Cause: the selector matches on `baseline-bf16` exactly (or some prefix), not "the most recent baseline-* row of any name." A baseline labeled `baseline-iter2-pre` is invisible to it.
- **Recipe.** Don't trust `comparison_vs_baseline` in the JSON output. Read `per_decision_grades` from BOTH the paired baseline JSON and the variant JSON, compute K1 (count of `k1_pass=true`) and regret (`sum(regret)`) yourself, then `regret_delta_pct = (variant_total_regret - baseline_total_regret) / baseline_mean_regret_or_eps * 100`. Or: run your paired baseline with `--variant baseline-bf16` exactly so the selector picks it up. Or: extend the bench to take an explicit `--baseline-row <timestamp>` flag.

### Single-shot equivalence gate is broken at temp=0.6 on gi=0-style multimodal decisions

- **What it is.** The gate (`K1_match >= 60% AND regret_delta within ±10%`) compares one variant run against one paired baseline. If any decision has a *multimodal sampling distribution* at temp=0.6 — multiple final_plays with materially different regret — the baseline lands on one mode and the variant lands on another, then the regret_delta blows past ±10% even when both are valid samples of the same distribution. **Confirmed firing in sprint 2 iter 3** (2026-04-28): two paired bf16 batch=5 temp=0.6 runs back-to-back produced bf16↔bf16 k1_pass agreement=100% and regret_delta=0% across the full subset, but `gi=0` history across 11 prior bf16 temp=0.6 runs shows final_play ∈ {25, 25, 2, 25, 25, 25, 25, 25, 2, 6, 19} with regrets {12.5, 0, 7.6, 9.8} — a true multimodal sampler. At temp=0, bf16 always picks play=2 (regret 0). Iter 2's Q4 verdict (gi=0 picked play=6, regret 7.63) was almost certainly not Q4 damage but Q4 sampling a mode bf16 also samples (the `q8-bf16-cont` run on 2026-04-27 hit play=6 with the same 7.632 regret).
- **Recipe.** Either (a) **pin `--temperature 0` for the gate** so K1+regret comparisons are deterministic — wall_s loses some realism, but the equivalence question becomes well-posed; or (b) **average K1+regret over N≥3 reseeded paired runs** at temp=0.6 and gate against the averaged distribution. Option (a) is the smaller code change and the right default for the perf sprint's gate.
- **Long-term fix.** Make the gate a two-track measurement: deterministic equivalence at temp=0 (must pass) AND temp=0.6 wall_s_per_decision (the perf metric). Don't gate on temp=0.6 K1/regret at single-shot.

### Misleading "loading bf16" log line

- **What it is.** Bench logs `[bench] loading Gemma 4 E2B (bf16, adapter=None)` regardless of what `--model-repo` was passed. The "bf16" is hardcoded printf text in `bench_decision_latency.py:962`.
- **Recipe.** Verify model from RSS instead — bf16 ~10–11 GB resident, Q4 PLE-safe ~4–5 GB. Or check the run's JSON output (`model_repo` field is correctly threaded through `args.model_repo` at line 971).
- **Long-term fix.** Patch line 962 to use `args.model_repo` instead of the hardcoded string. **Status:** patched and reverted in sprint 2 iter 4 (whole iter 4 commit was reset on gate failure). Re-apply on the next keep iteration.

### Q4 UD-MLX-4bit byte-equivalence claim doesn't survive temp=0

- **What it is.** Sprint 1 declared `unsloth/gemma-4-E2B-it-UD-MLX-4bit` byte-equivalent to bf16 on the 5-row corpus. Sprint 2 iter 4 (commit `6353da6`, reset) ran Q4 vs bf16 paired at temp=0 and found gi=0 deterministically flipped (bf16 play=2 regret 0 → Q4 play=6 regret 7.632). Same temp, same subset, deterministic both sides — the equivalence claim is wrong on this subset. The 44% wall win + ~50% peak-mem reduction is real, but the gate fails. **Sprint 2 iter 5 (commit `dac9d28`, reset) closed the bisect**: re-ran Q4 at batch=5 temp=0 (matching bf16's batch width); gi=0 still flips to play=6 with byte-identical regret 7.632 and identical 60% K1_match. The flip is **pure quant damage**, not batch-prefill numerics. UD-MLX-4bit's logits on gi=0 deterministically prefer play=6 to play=2; batch width is not load-bearing.
- **Recipe.** Don't trust prior byte-equivalence claims when bumping defaults to a quant model — re-verify at temp=0 against bf16 on the actual subset before declaring a new floor. UD-MLX-4bit is dead for this subset's gate; for the next quant attempt, try Q8 (`FakeRockert543/gemma-4-e2b-it-MLX-8bit`) — Q8 quant noise is typically ~16× smaller than Q4 and is the next floor candidate.
- **Long-term fix.** When promoting any quant set to "safe," include the temp=0 deterministic comparison artifact in the lever ladder note, not just a prose claim.

### gi=0 is a quant-fragile logit-cliff decision (the "~16× smaller noise" prior failed)

- **What it is.** Sprint 2 iter 6 (commit `1e82482`, reset) ran Q8 (`FakeRockert543/gemma-4-e2b-it-MLX-8bit`) at temp=0 batch=5 expecting Q8's ~16× smaller quant noise to byte-match bf16 on gi=0. **Q8 deterministically flipped gi=0 to play=6 with byte-identical regret 7.632 to Q4 UD-MLX-4bit.** Two different quant recipes (UD-MLX 4-bit, FakeRockert 8-bit) at two very different bit-widths produced the same wrong answer with the same regret. Q8 also added NEW damage at gi=104 (bf16 play=6 K1=True regret=0 → Q8 play=19 K1=False regret=0.025) — wider damage footprint than Q4. The "Q8 ≈ bf16" rule of thumb is unreliable on individual logit-cliff decisions; bit-width reduction does not commute with argmax across all decisions.
- **Recipe.** When a single decision flips deterministically across two unrelated quant recipes with byte-identical regret, treat the *decision* as quant-fragile, not the *quant set* as broken. Either (a) re-freeze the perf subset to exclude that decision before chasing quant wall wins, or (b) widen the equivalence gate to tolerate one K1-flip per subset (e.g. `K1_match >= 60% AND |regret_delta| <= max(10%, 1 fragile-decision worth of regret)`), or (c) pivot to non-quant levers (mlx-lm version bump, `mx.compile`, spec-decode) that don't perturb logits at all.
- **Long-term fix.** Subset-freeze protocol should include a "quant-fragility audit": for each frozen decision, verify its bf16 argmax survives a Q4 + Q8 perturbation. Decisions that flip across quant should either be excluded from the perf subset or marked as known-fragile so the gate weights them differently.

### temp=0.6 emit-token coverage probe is the wrong derivation for greedy temp=0 keep-set

- **What it is.** Sprint 2 iter 28 (2026-04-28, lever #16 Phase 1) probed Burl's emit-token distribution from a temp=0.6 belief-trajectory-rollout harvest and found 4,493 unique vocab IDs across 2.2M emit tokens — concluded "top-8192 covers 100%". Sprint 2 iter 29 (commit `0183abd`, reset) wired the prune (sliced lm_head + sampler-wrap LUT, mechanism verified by microbench at 5× head-matmul speedup), ran paired bf16 baseline + variant on subset_5 batch=8 temp=0 continuous → **gate failed**: K1_match=80% (pass), regret_delta=+64.3% (FAIL ±10%), wall=+2% (FAIL improvement). Two final-play flips (gi=0: 6→25, gi=104: 6→19), divergence compounded over 6-8 turns of tool dispatch.
- **Recipe.** When deriving a vocab-prune keep-set for **greedy decode** equivalence, the temp=0.6-sampled emit set is the WRONG set to measure. Greedy temp=0 picks strict argmax; temp=0.6 sampling has a non-trivial probability of skipping the top-1 token in favor of a slightly-lower-logit competitor. So tokens that ARE strict-argmax at some decode step might never appear in a temp=0.6 emit-trace. The right derivation: run a bf16 calibration pass that **logs every decode step's strict-argmax winner** (not just the emitted token after sampling) and union that set with the temp=0.6-emit set. If the union is still ≪ full vocab, ship the prune; if it's wider than expected, lever may be too narrow to be worth shipping.
- **Long-term fix.** Phase 1 derivation protocol for any vocab-restriction lever: (a) match the harvest's sampling policy to the bench's decode policy, OR (b) measure the larger of (sampled-emit, argmax-winner) at each decode step, OR (c) ship the lever conditional on a runtime safety check that detects "argmax outside keep_set" and falls back to full-vocab logits for that step. Option (c) requires the prune mechanism to be a *speed optimization* not a *behavior change* — bench wall savings come from common-case speed, gate compliance comes from worst-case fallback.

### Synchronous whole-lattice wax_museum precompute is not ANE overlap

- **What it is.** The rejected mk5-main-7v4 implementation added `--eager-tool-cache` by synchronously precomputing the entire [[wax-museum]] decision-local lattice before Gemma's first turn: `belief_trajectory()`, `explore_game(play)` for every legal play, both probes for every legal play, and four `ask_rule` topics. It did **not** use ANE/Core ML and did **not** overlap with Gemma prompt processing or decode. Clean stub A/B on `perf_subset_5`: lazy 3.6s wall vs eager 13.5s wall; same behavior (`5/5` committed, `5/5` probed, `4/5` bot-match, zero illegal/forced), but eager precomputed 79 responses, hit 10, wasted 69, and spent 13.5s precompute to save 3.8s of lazy tool work.
- **Recipe.** Do not implement "precompute" as a blocking before-turn whole-lattice cache. First prove the backend (`ANE/CoreML`, `MPS`, or `CPU`) with instrumentation, then scope work by gate state: INITIAL may schedule `belief_trajectory` or legal-play summaries; AFTER_EXPLORE may schedule probes only for the explored candidate; AFTER_PROBE should avoid speculative work unless it is already hidden behind decode. Cache misses must fall back to lazy execution.
- **Long-term fix.** Treat ANE-overlapped tool work as a sidecar design problem, not a registry memoization problem. Required stats: scheduled/completed/hit/wasted grouped by tool and gate state, backend used, precompute wall, and hidden/overlapped wall. The pass condition is paired clean `perf_subset_5` wall no worse than lazy plus unchanged legal/final/forced sanity.

## Profiling / instrumentation footguns

### Apple Metal capture (`mx.metal.start_capture`) blows up to multi-GB on a real bench run

- **What it is.** mlx exposes `mx.metal.start_capture(path)` / `stop_capture()` with `MTL_CAPTURE_ENABLED=1` to record a `.gputrace` file for Xcode. Apple Metal capture serializes every command buffer dispatch and writes the binary state into the trace bundle (the `.gputrace` is a directory with thousands of `MTLBuffer-*` files, often hardlinked to share state). Sprint 2 iter 13 (2026-04-28, commit `e9f1e6c`) wrapped a 5-decision continuous bench run in `start_capture` and the trace ballooned to **5.3 GB at 6 minutes elapsed** with no signs of stopping (iter 11's clean baseline of the same workload is 41s). Killed before it filled the disk; the .gputrace was unrecoverable to Xcode anyway since the host runs macOS 26.4.1 (issue [#2846](https://github.com/ml-explore/mlx/issues/2846) — "metal_capture does not capture working GPU trace in MacOS 26.1+", same on .4.x).
- **Recipe.** Don't wrap a multi-second whole-bench run in `mx.metal.start_capture`. If you need kernel-level profiling, two options: (a) capture a SINGLE forward pass (~10-50ms) — small enough that the trace fits in low MB and replay won't take forever; (b) skip the .gputrace entirely and use a `KernelCounter` analytical breakdown (call-counts × per-call bandwidth-bound cost) — reusable from iter 13's `bench_decision_latency.py:--kernel-audit` flag. Option (b) is what produced the iter 13 5-row breakdown without ever opening Xcode.
- **Long-term fix.** None at the playbook level. Apple's Metal capture is structurally not designed for whole-application traces of long-running workloads; it's a single-frame-snapshot tool.
- **Caveat — instrumentation overhead.** Even WITHOUT `MTL_CAPTURE_ENABLED=1`, the iter 13 kernel-counter wrappers (per-call Python wrapping of `nn.Linear.__call__` at the class level) added ~38% wall overhead on the bf16 continuous bench (56.5s instrumented vs 41s clean). Use the `--kernel-audit` flag only when needed, never for paired wall comparisons against an un-instrumented baseline.

### `nn.Module.__call__` cannot be overridden at the instance level

- **What it is.** Setting `instance.__call__ = hooked_fn` on an `nn.Linear` (or any `nn.Module`) silently does nothing — Python's special-method lookup for `__call__` skips the instance dict and goes straight to the class via type(...). Sprint 2 iter 13 hit this when adding `KernelCounter` hooks; the original "set `layer.__call__ = wrapped`" approach left every Linear unwrapped (zero counter increments, but no visible error). Wasted ~10 minutes of debug time on an audit run.
- **Recipe.** To intercept `nn.Linear.__call__` per-instance, replace the CLASS-level `__call__` with a wrapper that reads a per-instance tag (`getattr(self, "_audit_kernel_name", None)`) and falls through to the original for untagged instances. Tag the instances you care about during install. Reverse on teardown by restoring the original class-level `__call__`. See `bench_decision_latency.py:install_kernel_hooks` for the working recipe.
- **Long-term fix.** None — this is a Python-language constraint, not an MLX issue.

## Stuck worker

### Symptom: worker silent for 2+ `/loop` fires

- **What it is.** The worker is wedged — could be an mlx-lm internal hang, a stuck subprocess, an infinite loop in the bench, or the worker reasoning itself into a corner. The orchestrator's `/loop` fires don't get a `SendMessage` reply.
- **Recipe.**
  1. `SendMessage` the worker once more with a tight question (e.g. "respond with one word: alive?").
  2. If no response: `TaskStop` the worker.
  3. Respawn a fresh worker with the same variant via the spawn template in [[perf-sprint]]. The new context may avoid whatever wedge the previous one hit.
  4. If the same variant wedges twice in a row: log a `crash` row to `results.tsv` with description "wedge — variant skipped," and pick a different variant.
- **Long-term fix.** None at the playbook level. If a specific variant or technique consistently wedges, document it as a closed lever in [[perf-sprint-levers]] with the wedge as the closure reason.

### `FileNotFoundError` on `gus/adapters/v3_consistency_10000g.pt` mid-decision when running the bench from a worktree

- **What it is.** Two adapter-resolution paths in the bench disagree when the bench runs from a `git worktree`. The bench top-level (`bench_decision_latency.py:1248-1258`) explicitly falls back to the absolute main-checkout path and pre-loads gus successfully, populating `belief_trajectory._load_gus_cached`'s lru_cache keyed on the main-checkout string. But `belief_trajectory.DEFAULT_ADAPTER` is computed from `Path(__file__).resolve().parents[2] / "gus/adapters/v3_consistency_10000g.pt"` — i.e. **worktree-relative**. The first time a stream calls `belief_trajectory()` as a tool with `adapter_path=None`, `load_gus()` falls through to that worktree path, misses the lru_cache (different string key than the pre-load used), and `torch.load` aborts with `[Errno 2] No such file or directory`. Bench setup looks healthy (corpus loaded, oracle loaded, gus pre-loaded, model ready) and decision dirs are created up-front by continuous mode — the bench dies on `decision_0`'s first turn. Confirmed firing in sprint 2 iter 15 (2026-04-28) when `gus/adapters/` did not exist in the perf/aggressive worktree.
- **Recipe.** From the worktree, symlink the adapter into the worktree-relative path so both resolution paths converge:
  ```bash
  mkdir -p .claude/worktrees/<name>/gus/adapters
  ln -s /Users/jason/code/mk5-main/gus/adapters/v3_consistency_10000g.pt \
        .claude/worktrees/<name>/gus/adapters/v3_consistency_10000g.pt
  ```
  Pre-flight check before any iter that runs from a worktree: `ls <worktree>/gus/adapters/v3_consistency_10000g.pt`. The .gitignore on `gus/adapters/` means a fresh `git worktree add` will never carry the directory across.
- **Long-term fix.** Make `belief_trajectory.DEFAULT_ADAPTER` use the same fallback list as the bench top-level (try worktree-relative, then `/Users/jason/code/mk5-main/gus/adapters/...` absolute). One ~6-line edit in `burl/tools/belief_trajectory.py`. Or thread the resolved path through the tool registration so callers don't fall back to the module default. Either fix removes the trap permanently.

## Append a trap

When a sprint discovers a new failure mode, add a section with: symptom, what it is, recipe.

## Links

[[perf-sprint]] [[perf-sprint-loop]] [[perf-sprint-levers]] [[perf-sprint-history]]
