# Work Beads

Read @forge/ORIENTATION.md

Currently, the seeds are generating.  Test the pipeline with just a few seeds of local data in `/data/shards-marginalized/train`. 

## Loop

1. Execute `scripts/get_ready_by_epic.sh t42-tsvp` to get one bead id that is ready
2. Implement that bead to completion
3. Follow entire beads close protocol completely.  
4. Once the bead is closed, update forge/CLAUDE.md with each tool call failure and remedy 

## Done

When there is no more work in the epic:

<promise>BEADS COMPLETE</promise>
